# ENGINE MAP

> **Generated** by `scripts/engine_map.mjs` — re-run it rather than editing this file.
> Mechanical columns are derived from the static import graph, which is complete: `app.js` contains **zero dynamic imports**.
> **`purpose`, `player-visible surface` and `what makes it fire` are AUTHORED** in `scripts/engine_map.authored.json` and are never overwritten by a regeneration.

**105 modules · 86/105 described.** Two flags carry the defect shapes this map exists to catch. **`player-visible surface: NONE`** (L1) is how a capability gets built, tested, and never met. **`what makes it fire: NONE` on a module that DOES have a surface** (L2, "permission isn't initiative") is how a capability the player can reach still never happens — the teacher gate that never fired.

**How each derived column is measured** — so the columns can be trusted or corrected rather than believed:

- **depends on / depended on by** — static `import` statements. Complete: `app.js` has zero dynamic imports.
- **reach** — transitive closure of *depended on by*. `app.js` counts once if reachable at all, directly or through a chain.
- **content it reads** — literal `*.json`/`*.md` paths, `CONTENT.*` keys, and content-schema fields harvested from the real location and NPC corpus. **Excluded:** field names that collide with JS members (`map`, `name`, `id`, …) and fields ≥9 modules read, which discriminate nothing. 51 fields qualify.
- **GM verbs** — from `applyTurn`'s dispatch in `app.js`, *not* from imports: each `turn.<verb>` handler block is walked and the verb attributed to whichever module owns the functions called inside it.

| module | purpose | player surface | what makes it fire | exports | depended on by | reach | content it reads | GM verbs |
|---|---|---|---|---|---|---|---|---|
| `engine/namematch.js` | The one name-resolution primitive — slugify and fuzzy-match an entity the same way everywhere, so codex, quests and inventory can never disagree about what the GM just named. | Indirect but constant: every id the player sees, and the quest panel's "couldn't match" note when the GM names something that does not resolve. | Always — every entity the GM names is resolved through it on the turn it is mentioned. | 7 | `affiliation.js` `art.js` `assignments.js` `authormode.js` `battle_turn.js` `battleprompt.js` `braids.js` `canon.js` `codex.js` `companions.js` `corrections.js` `earnedpower.js` `encounters.js` `entityDetail.js` `facts.js` `feed.js` `generate.js` `gm.js` `holdings.js` `imageprompt.js` `inventory.js` `latentarcs.js` `legends.js` `npcs.js` `party.js` `personalArc.js` `places.js` `practice.js` `pressure.js` `progression.js` `quests.js` `random_encounters.js` `reconcile.js` `wake.js` `worldtick.js` `app.js` | **47** | — | `offer` |
| `engine/skilltree.js` | Skill legibility and gating: tiers, attribute gates, and what is learnable now versus later. | The skill wheel and skill graph — tier rings, locked/unlocked state, and the reason a locked ability is locked. | Opening the skill wheel or graph; a learn/deepen action re-reads it in place. | 26 | `battle_turn.js` `company.js` `gm_registry.js` `intensity.js` `npcsheet.js` `progression.js` `traditions.js` `wheelgeom.js` `app.js` | **41** | — | — |
| `engine/ladder.js` | *— unstated —* | *—* | *—* | 11 | `assignments.js` `company.js` `gm_registry.js` `incapacitation.js` `progression.js` `reconcile.js` `reputation.js` `resolve.js` `sense.js` `worldtick.js` `app.js` | **36** | — | — |
| `engine/traditions.js` | The Great Circle geometry — the 24-station ring, neighbours, antipodes and distances — read from content, never hardcoded. | The skill wheel's ring layout and the tradition labels shown on abilities and standing. | Always — the ring geometry is read whenever an ability, domain or standing is shown. | 21 | `braids.js` `functions.js` `progression.js` `quests.js` `reconcile.js` `standing.js` `state.js` `app.js` | **35** | `traditions_v2.json` | — |
| `engine/earnedpower.js` | SNG-251 §2c/§4: the EARNED-POWER ECONOMY. Answers only three questions — how much power may this item hold (a ceiling that is a FUNCTION of level + craft rank, per Erik), may it evolve right now (~1/day per item), and is this proposed grant within bounds. Replaces the old blanket ban ("itemUpdates does NOT grant new power") with the rule that ban existed to enforce: no UNEARNED power; earned power is explicit and clamped. | The item card's mechanical sheet — what an evolved item GRANTS, where each grant came from, and what it explicitly cannot do. Erik's "be explicit about what that translates to in game mechanics", literally on the item. | Any item evolution: the GM's itemUpdates carrying grants, a deriveItem split, or the player's own ✦ Evolve action. All three pass through the same ceiling and the same daily budget, so no route can grant more than another. | 7 | `inventory.js` `app.js` | **30** | — | — |
| `engine/recurrence.js` | Prestige-driven recurring challengers — a bound arc draws duel challengers as the character's renown rises, the swordsman's paradox as an engine: prestige feeds challengers feeds prestige. | A returning challenger arriving in the fiction, scaled to your renown band. | A renown (deeds-driven reputation) band crossing plus a challenger cooldown — the duel runs as an ordinary SNG-098 skill battle. | 11 | `reputation.js` `app.js` | **30** | `.challengers` | — |
| `engine/inventory.js` | Items as first-class objects the character can carry and USE — normalized losslessly from legacy string saves, with quantities, uses and dedupe. | The inventory panel and the INVENTORY block; the receipt when an item is used or consumed. | A GM itemUpdates op writes the pack; using an item in a scene reads and decrements it. | 33 | `authormode.js` `battle_turn.js` `corrections.js` `encounters.js` `entityDetail.js` `gm_registry.js` `playerprofile.js` `reconcile.js` `app.js` | **29** | `sub_attribute_ladder.json` `.provenance` | `deriveItem` `itemUpdates` |
| `engine/reputation.js` | Deeds are the source of truth and reputation is a VIEW over them — a community's opinion is the sum of the deeds it knows about. | The standing list on the character screen (band per people). Empty for most players today — the gap BATCH-12 §3 exists to close. | A deed is recorded (GM deeds op or an encounter), and the standing list reads the view. Deeds fire heavily; the mark-ops that would move people-standing do not (SNG-179). | 8 | `gm.js` `npcs.js` `progression.js` `quests.js` `standing.js` `worldtick.js` `app.js` | **29** | — | `deeds` |
| `engine/wake.js` | SNG-204: the wake engine — a resolved SIGNIFICANT outcome (world/tradition/regional tier, or any arc move) leaves a WAKE: a generation-ready trace of what changed (the applied effects), which way it pushes the world (the moved arc's authored pressureOnAdvance — the inference seed), its scale, and the neighbouring arcs it presses on (connectsTo). Closes the loop the world had open: consequences used to land durably and then stop; now they become the next thing. Owns the record + lifecycle + the cheap path; the model-generation path (a full new quest minted FROM a wake) is Phase 2 and reads these open wakes. | The GM weaves the next thread OUT of a resolved consequence (a faction reacting, a place coping, a person seizing the moment) — surfaced as the WAKES block. A wake's lean also moves the connected arcs on the world-map readout; an unengaged wake's window closing shows as world-tick news. | resolveStructuredQuest calls createWake after applying a significant outcome's effects; the wake immediately leans (signed by the arc move's direction) on each existing connectsTo neighbour via worldState.wakeArcPushes, which worldtick.arcStageNow folds into the net so the neighbour's stage actually moves. The world-tick calls decayWakes so open wakes fade + close on schedule. gm_registry surfaces wakesForGM. | 9 | `gm_registry.js` `quests.js` `worldtick.js` `app.js` | **29** | — | — |
| `engine/playerprofile.js` | Player identity across characters, and the earned play-style (tendencies, aptitudes) that accrues on each character from how it is actually played. | The profile screen's play-style readout, and the character-select roster. | Every resolved action accrues play-style; the profile screen and character-select read it. | 23 | `art.js` `canon.js` `feed.js` `app.js` | **28** | — | — |
| `engine/quests.js` | Quests as typed state the GM drives through clamped ops — the model proposes, the engine adjudicates and refuses in the open. | The quest panel: active quests, stage progress lines (✦ …), the resolution choice when every stage is done, and refusal notes when the GM names the wrong stage. | A GM stageOps/questUpdates op, or the quest-log 'Take it on' button. SNG-162 gave it a play-time trigger; before that a structured quest could only be closed from the fix button. | 25 | `battle_turn.js` `codex.js` `generate.js` `gm_registry.js` `npcs.js` `progression.js` `reconcile.js` `app.js` | **28** | `.waygateTier` `.want` `.boundToCharacter` | `holdingOps` `questUpdates` `stageOps` |
| `engine/art.js` | Images for characters, locations, items, NPCs and big moments, from either a static file or a generated prompt, behind one seam. | Every image in the app: location art, portraits, item art, the moment gallery. | Any record without an image, at first render or on a bond crossing (ensureBondPortraits) — never eagerly, so a passing face costs nothing. | 44 | `battleprompt.js` `corrections.js` `npcs.js` `state.js` `app.js` | **27** | `.descriptionSeed` `.encounterFlavor` `.provenance` `.voiceHints` | `imagePrompt` |
| `engine/codex.js` | The character's knowledge graph — what they have LEARNED as linked, typed topics (mysteries, factions, lore, people, places) with accumulating facts, written only through clamped ops. | The codex screen and the CODEX block, which lets the GM have the character ACT on what they know instead of re-explaining it. | A GM codexUpdates op writes a topic or fact; auto-consolidation (SNG-153) folds duplicates past a threshold; the codex screen reads it. | 24 | `gm_registry.js` `npcs.js` `places.js` `reconcile.js` `worldtick.js` `app.js` | **27** | `.parentId` | `codexUpdates` |
| `engine/names.js` | One name, one home — a display name lives on the record its id points at, and every reference resolves it rather than spelling it. | Every authored name a player reads: a rename changes the record and the prose follows, and SNG-111's progressive naming renders the same person differently to someone who has learned their name. | Prompt assembly and any render of authored prose — a {{kind:id}} token resolves to the current display name. Always, but a no-op on strings without a token. | 14 | `gm_registry.js` `npcs.js` `worldtick.js` | **26** | `.wants` | — |
| `engine/npcs.js` | NPC permanence — everyone met, authored or invented mid-scene, gets a durable registry entry the GM writes to through clamped npcUpdates. | The people list and each NPC's detail card: who they are, where you met, what has passed between you. | A GM npcUpdates op writes the registry; the people list and detail card read it. NB (SNG-179): the mark-a-relationship ops fire far less than the list-shaped ones. | 36 | `companions.js` `company.js` `corrections.js` `entityDetail.js` `gm_registry.js` `reconcile.js` `worldtick.js` `app.js` | **25** | `rules/minted_names.json` `.questSeeds` `.homeLocation` `.wants` | `npcUpdates` `relationshipDeltas` |
| `engine/company.js` | The unified company: companion, trainer, liaison, partner and ally are stacking ROLES a person in your party holds, each wired to the system that already implements it. | The company panel — who travels with you and what each one is to you. | Recruiting a met NPC, and — SNG-175 — a bonded teacher's next-step offer now surfaces through teachersForGM. The gate existed for levels; the initiative did not until then. | 17 | `gm_registry.js` `progression.js` `standing.js` `worldtick.js` `app.js` | **22** | `CONTENT.npcs` `.teaches` `.curriculum` `.liaisonFor` | `partyOps` |
| `engine/craftmechanics.js` | SNG-263: THE CRAFT MECHANICAL BODY. Erik: 'every function described needs a matching game mechanic that can be verified.' The audit found 285 crafts carrying `functions` and not one field for damage, healing, duration, range, area or rank — of 24 verbs only strike and break did anything, so heal on 31 crafts healed nothing and reveal, the largest family at 114 crafts, did nothing at all. This resolves a verb to its family's effect-SHAPE and then to real numbers: magnitudes, Erik's tier ladder (T-II doubles, T-III beats linear, T-IV/V flagged special), the per-rank delta, and per-intensity scaling. Damage is ROLLED, calibrated so a T-I strike's maximum can kill a 5-health peer and its average takes 2-3 hits. | NONE directly — the player meets it as crafts that finally DO what their description says: a ward that actually reduces incoming harm, a heal that restores health, a bind that applies a named penalty, and a rank-up that changes a number they can point at. | Pure and injected: every magnitude, multiplier and band comes from rules/craft_mechanics.json. NEVER invents a number, and NEVER reports a zero for a dimension a verb does not use — 'no range' and 'range 0' must not look alike. | 22 | `battle_turn.js` `braids.js` `progression.js` `skill_battle.js` `app.js` | **22** | — | — |
| `engine/progression.js` | Character growth with numbers you can point at — sub-attributes, levelling, ability acquisition and the receipts for each. | Level-up prompts, the attribute allocation screen, new-ability offers, and the ✦ receipt lines when something is earned. | A resolved action that crosses a threshold, a level-up, or a GM offerAcquisition/offerPromotion — the player commits on their own screen (Law 9). | 40 | `authormode.js` `backfill.js` `battle_turn.js` `braids.js` `corrections.js` `gm.js` `gm_registry.js` `practice.js` `app.js` | **20** | — | `markDefiningMoment` `newAbility` `offerPromotion` |
| `engine/melee.js` | *— unstated —* | *—* | *—* | 27 | `caravan.js` `encounters.js` `gm_registry.js` `holdings.js` `reconcile.js` `skill_battle.js` `app.js` | **15** | — | `bandOps` |
| `engine/worldmap.js` | The data-driven map foundation: stable auto-positioning for every location including ones minted mid-play, plus the three zoom tiers. | The map screen and its World / Region / Location tier bar. | Opening the map (any of three tiers), and SNG-180 wired walkingDays to the place card so the world scale is met on travel. | 22 | `gm_registry.js` `holdings.js` `journey.js` `presence.js` `reconcile.js` `substrate.js` `app.js` | **15** | `CONTENT.locations` `.connections` `.poleIntensity` `.worldPos` | — |
| `engine/capabilities.js` | *— unstated —* | *—* | *—* | 8 | `battle_turn.js` `npcsheet.js` `app.js` | **14** | — | — |
| `engine/threat.js` | CCODE-52: the threat balance system. Erik's ruling — a region is never one level range; the region supplies the CAST, the player's built power supplies the MEAN the encounters revolve around, and both tails are real. characterPower (what you are actually worth), threatBand (how a foe reads AGAINST YOU, relative), isRelevantThreat (a boar you have outgrown stops being an encounter unless something makes it special), sampleThreat (the draw, with a genuine upper tail), applyVariant (what makes an outgrown thing matter again). Pure. | The pre-fight read — the rung a foe sits on for THIS character (beneath notice → beyond you) and the counsel that goes with it, plus which encounters the world offers you at all. | Every encounter offer (the pool is drawn around your power) and every appraisal of a foe before you commit. | 7 | `random_encounters.js` `sense.js` `app.js` | **14** | — | — |
| `engine/npcsheet.js` | *— unstated —* | *—* | *—* | 15 | `battle_turn.js` `gm_registry.js` `holdings.js` `worldtick.js` `app.js` | **13** | `npc_interiority.json` `.physicality` `.canStrike` `.closed` | — |
| `engine/waygate.js` | The waygate network and its hub at the Crossing, including routing a GM's moveTo to a real gate rather than letting it mint a destination. | The waygate travel control and the destination list when standing at a gate. | Standing at a gate (the travel control), and a GM moveTo that names a gate is routed through it rather than minting a destination. | 13 | `gm_registry.js` `holdings.js` `journey.js` `app.js` | **13** | `.waygate` `.waygateTier` `.waygateDefaultTo` `.waygateHub` | — |
| `engine/companions.js` | Traveling companions — present in every scene, voiced by the GM, mechanically helpful where their nature fits; the character carries only the ids, definitions are content. | The COMPANIONS block and the companion detail card — who travels with you and what they can do. | Always, for a present companion; an evolution threshold deepens one (SNG-175). | 18 | `backfill.js` `battle_turn.js` `gm_registry.js` `reconcile.js` `app.js` | **12** | `.voiceHints` `.knowledge` `.appearance` `.assistTags` | — |
| `engine/economy.js` | SNG-302: prices a carried thing against where it is — worth band times local need times scarcity, with need dominating and `none` a hard zero. | What a trader will say a thing is worth, and the refusal when something is not for sale at any price. | Carrying anything in a region with an economy; an arc stage's priceShift moves the local need underneath it. | 8 | `gm_registry.js` `holdings.js` `state.js` `app.js` | **12** | — | — |
| `engine/purse.js` | *— unstated —* | *—* | *—* | 18 | `caravan.js` `holdings.js` `reconcile.js` `app.js` | **12** | `economy.json` | — |
| `engine/substrate.js` | The SECOND difficulty map — the nanite lattice varies in density by place and each tradition is tuned to a band of it, so where you stand changes what you can do. | The success-chance readout before acting (the penalty is folded in, per SNG-116) and the thin/crowded lattice note the GM works into prose. No named receipt of its own. | An ABILITY action at a place (weapon actions are substrate-free, SNG-089). The success-chance readout and the thin/crowded note are the surfaces; SNG-183 note — the geographic FIELD is precomputed at load but the ruler question is open. | 30 | `gm_registry.js` `holdings.js` `reconcile.js` `state.js` `worldtick.js` `app.js` | **12** | `power_sources.json` `the_substrate.json` `CONTENT.schools` `.substrateSource` | `adoptSchool` |
| `engine/braids.js` | SNG-196: the generative core made real — co-activating two crafts in one action EARNS a braid past a threshold, a new full-schema ability minted onto the character that neither parent could do alone. No authored recipe required (that absence was the gap); tier scales with the power of the two crafts braided. SNG-197 §4: isLegalEmergent enforces the emergent capability against the real 24-verb vocabulary — a hallucinated verb is rejected, not accepted-and-logged. | A new craft on your sheet, named by you or the game, with its own rank tree — and the mint MOMENT when a pairing you have leaned on becomes a braid, with the one thing it can now do that neither parent could. | A pairing co-activated past BRAID_RIPEN_AT with both crafts still held (mintableBraidsFor); the reconcile v14 backfill mints the braids a save already earned. GENERATIVE — no recipe gate. | 10 | `generate.js` `recipes.js` `reconcile.js` `app.js` | **11** | — | — |
| `engine/holdings.js` | *— unstated —* | *—* | *—* | 62 | `caravan.js` `gm_registry.js` `reconcile.js` `worldtick.js` `app.js` | **11** | `.descriptionSeed` `.substrateSource` `.reactsToReputation` | `debtOps` `holdingOps` |
| `engine/random_encounters.js` | The living valley — on travel, rest, arrival or world-tick the engine MAY roll one flavored encounter, weighted by the place's dangerLevel and tags, across a tonal spectrum that is never grim by default. | The woven SOMETHING HAPPENS beat when a roll lands. | A travel/rest/arrival/tick roll (SNG-075) — weighted by the location, and never announced as a system event. | 21 | `battle_turn.js` `state.js` `app.js` | **11** | `CONTENT.randomEncounters` `.dangerLevel` | — |
| `engine/affiliation.js` | The one answer to 'what kind of being is this person, and what do they practise' — read from their own role, skills and region with provenance, so the two mint paths can never disagree about it again. | Indirect: every teacher who can now teach and every people who can now credit you — a met NPC's craft becoming legible is what lets standing move and a mentor mentor. | Meeting anyone in play (npcs.js applyNpcUpdates), minting anyone (generate.js), and the reconcile v11 backfill over people already in the save. Before SNG-185 the meet-path stamped nothing, which is why registry-only people had no craft. | 5 | `generate.js` `reconcile.js` `app.js` | **10** | — | — |
| `engine/functions.js` | The function-family axis over the skill system — inverts the authored verb→family vocabulary (8 families, 24 verbs), measures a character's coverage, and recommends skills that fill the gap. | The function-gap line in the toolkit block, and the coverage read where a kit's missing family is named. | Assembling the toolkit (the gap), and any place the engine reasons about what KIND of act an ability performs. | 13 | `coliseum.js` `combatants.js` `encounters.js` `gm_registry.js` `toolkit.js` `app.js` | **10** | `function_vocabulary.json` `CONTENT.classArchetypes` | — |
| `engine/legends.js` | The world's great figures — heroes witnessed and villains scaling from riffraff to arc, so a world feels large when greatness passes through it. The engine detects the beat and GOVERNS the rarity; the GM narrates. | The LEGEND block — a great figure surfacing in the fiction, governed so it stays rare. | A qualifying beat (maybeLegendDetail governs the rarity); rides the generate + weight/recurrence + codex machinery. | 9 | `gm_registry.js` `state.js` `whois.js` `worldtick.js` `app.js` | **10** | `.homeLocation` `.wants` `.legends` | — |
| `engine/martial.js` | *— unstated —* | *—* | *—* | 8 | `reconcile.js` `state.js` `app.js` | **10** | — | — |
| `engine/personalArc.js` | Every character's PRIMARY arc, generated from the backstory they wrote — the bound-arc shape SNG-132's runtime surfaces and paces, so a player's own throughline is a real quest that is never foreclosed. | The personal-arc quest in STRUCTURED QUESTS, surfaced and paced like any bound arc. | Character creation generates it from the bio; a beat that touches it advances it; reconcile backfills it onto older saves. | 9 | `reconcile.js` `app.js` | **10** | `.disposition` | — |
| `engine/reconcile.js` | Save and content migration — versioned, idempotent, one-time steps that bring a character or content authored by an older engine up to current, so nobody loses a save to a schema change. | The one-time '✦ …' notices on load when a migration grants a character what a newer feature owes it. | Loading a save (or content) whose reconcile-version trails the engine — each step runs once, in order, then never again. | 5 | `state.js` `app.js` | **10** | `ability_rename_map.json` `.connections` `.poleIntensity` `.worldPos` | — |
| `engine/recovery_snapshots.js` | Diffs of save branches the sync overwrote, carried in code so reconcile step 41 can merge them into every copy of the save at its next load — a committed save file loses the race against an open tab; a step runs inside it. | The reconcile note on load: what was recovered — items, people, topics, facts, holdings — and that nothing was removed. | Step 41 on any character whose id matches an entry's forId; mergeRecovery is idempotent, so a copy already whole gets no note. | 1 | `reconcile.js` | **10** | — | — |
| `engine/recovery.js` | Merges a recovery snapshot back into a save, losing nothing from either. The sync kept the losing copy of every both-advanced conflict in localStorage and nothing ever listed, read or merged one — a player who lost a branch had it saved and unreachable. | Settings → Recovery copies: each snapshot the sync kept, with when and what it holds, and one verb — Merge into my save — that adds what the copy has and this save lacks, and removes nothing. | A both-advanced sync conflict writes a snapshot; the player merges it from Settings. Merge is idempotent, refuses another character's snapshot, and treats level and xp as floors. | 2 | `reconcile.js` `app.js` | **10** | — | — |
| `engine/standing.js` | How a people regards you — seeded at creation from who you are, drifting with the company you keep, and moved by narrated acts the engine adjudicates rather than the model. | The standing list on the Chronicle (band per people and settlement), the ✦ receipt when a band changes, and the GM's own knowledge of your welcome. | Seeded at character creation; drips per in-game day from the company you keep; a GM standingOps op moves it from a narrated act. BATCH-12 §3 — before it, only a structured quest with a people-effect ever wrote peopleDisposition, which is why Erik's standing screen sat empty. | 11 | `gm_registry.js` `reconcile.js` `app.js` | **10** | `.teaches` `.liaisonFor` | — |
| `engine/state.js` | Content-pack loading and save/load — localStorage is authoritative, GitHub sync is optional on top. | Boot ("Loading the Valley"), the save/load controls, and the recovery-slot list when a save fails to write. | Boot loads content; every save/load and the recovery path fire it. Always-on infrastructure. | 36 | `generate.js` `gm_registry.js` `reconcile.js` `whois.js` `app.js` | **10** | `content/packs/core/manifest.json` `content/packs/valley/manifest.json` `rules/location_affinities.json` `location_affinities.json` | `moveTo` |
| `engine/titles.js` | SNG-287: builds a figure's or player's earned title from their own record — pattern plus slots, where every slot must be fillable or the pattern is skipped. | The name spoken when someone rises ("Neth, Who Turned the Bleed, is called legendary this season"), and the title kept on the figure afterwards. | A promotion, when the bearer's deed record can fill every slot of an authored pattern; most rises carry no title, which is what keeps one meaning something. | 5 | `state.js` `worldtick.js` | **10** | — | — |
| `engine/worldtime.js` | The two clocks — character time (days and season, the player's own) and the world Kept Count (a shared, monotonic mark spoken in the local people's idiom), in different units so neither invents a date for the other. | The CURRENT TIME narration and the count rendered in the local idiom; the story/real time-mode setting. | Always — readClock runs every turn and the count advances about once per real hour; SNG-191 removed the invented day-number. | 17 | `gm_registry.js` `state.js` `worldtick.js` `app.js` | **10** | `world_clock.json` `CONTENT.worldClock` | — |
| `engine/resolve.js` | Deterministic d100 action resolution — the model never rolls; this turns an action and its modifiers into a rolled degree of success the whole engine reads. Pure. | Indirect but constant: the success-chance readout before acting and the outcome degree the narration is handed. No panel of its own — it is the dice under everything. | Every action that resolves — and every encounter, gambit and skill-battle round routes its roll through here. | 7 | `gambit.js` `gm.js` `sense.js` `skill_battle.js` `app.js` | **9** | — | — |
| `engine/targeting.js` | *— unstated —* | *—* | *—* | 9 | `sense.js` `skill_battle.js` | **8** | `jsconfig.json` | — |
| `engine/borncontract.js` | SNG-250 §4: THE UNIVERSAL BORN-WHOLE GATE. One checker, every type, keyed entirely by the consumer map (CONTENT.consumerContract) — asks whether a record is WHOLE (every field a real consumer reads carries a value) and CONCRETE (that value is something the rules can ACT on, not a word). Adding a type means declaring its contract, never writing gate code. | NONE directly — the player meets it only as generated content that is never hollow: no un-fightable monster, no skill that resolves to nothing, no item that cannot change a roll. | Every generate() mint (engine/generate.js, after the schema repair/floor pass — a CRASH verdict rejects the record, anything softer is stamped on _gen.contract), and the CI shape-sweep over AUTHORED content (tests/content_ci.mjs). Deliberately the SAME function on both paths, so authored and generated content are held to one bar. | 7 | `generate.js` `app.js` | **7** | `CONTENT.consumerContract` | `newAbility` |
| `engine/combatants.js` | *— unstated —* | *—* | *—* | 15 | `encounters.js` `group.js` `skill_battle.js` `app.js` | **7** | `.assistTags` `.canStrike` | — |
| `engine/genschema.js` | A dependency-free JSON-Schema checker and repair helper for the generative pipeline — exactly the subset the derived npc/location/arc schemas use, so a grown entity is shaped like an authored one. | NONE directly — it validates generation; the player meets it only as generated content that never arrives malformed. | A generateRequest — generate.js validates (and auto-repairs) the model's output against the derived schema before minting. | 3 | `generate.js` | **7** | — | — |
| `engine/practice.js` | Competency as the residue of attention — an engine-owned ledger of ability use and co-activation that unlocks ranks at zero skill-point cost. | Aspiration progress and the unlock notices when practice crosses a threshold. | Every ability use records to the ledger; an aspiration or rank threshold crossing surfaces the unlock. SNG-173 made the toolkit's quiet-craft pool recency-based so it stops draining. | 15 | `backfill.js` `battle_turn.js` `evolution.js` `gm_registry.js` `app.js` | **7** | — | — |
| `engine/claude.js` | Anthropic API transport, with MODEL_MAP as the single source of truth for task→model routing and a token/stop_reason log per call. | NONE directly — but every latency the player feels and every API error card comes from here. | Every callClaude — the play loop, ask-the-GM, generation, world-tick. NONE player-facing directly; it is the transport. | 7 | `gambit.js` `gm.js` `imageprompt.js` `worldtick.js` `app.js` | **6** | — | — |
| `engine/conditions.js` | *— unstated —* | *—* | *—* | 8 | `battle_turn.js` `skill_battle.js` `app.js` | **6** | — | — |
| `engine/damagetypes.js` | *— unstated —* | *—* | *—* | 7 | `skill_battle.js` | **6** | `craft_mechanics.json` | — |
| `engine/death.js` | SNG-209: death as a retrievable STATE, not a delete — a fallen figure enters a death-state at a computed DEPTH (threshold → near-dark → deep-dark → SEALED) that sinks on the world clock while untended, so a loss can be answered for a while and then becomes permanent. | The “THE DEAD WHO ARE NOT GONE” GM block (who can still be reached, and how near the wall they are), and the author-panel return/seal control. | worldtick stamps a qualifying (epic-tier) kill into a death-state and runs deepenDeaths on the world count; gm_registry surfaces reachableDeadForGM every turn; authormode enacts a return or a seal. NOTE (SNG-209 follow-up): only epic kills stamp a deathState today, so ordinary dead NPCs read as reachable with no clock. | 13 | `authormode.js` `battle_turn.js` `caravan.js` `gm_registry.js` `worldtick.js` `app.js` | **6** | `docs/HOW_IT_WORKS.md` `.legends` | `deathOps` |
| `engine/generate.js` | The ONE generative path — authors a new NPC, location or arc in-grain for its locale, validates against the derived schema, resolves before minting so it never duplicates, and persists it. | Any place or person that did not exist until the fiction needed it; the player meets these as ordinary world content. | The fiction naming a person or place that does not exist yet — a GM generateRequest, or the mint-on-move path. Never eager. | 31 | `canon.js` `chronicle.js` `gm_registry.js` `worldtick.js` `app.js` | **6** | `CONTENT.consumerContract` `.connections` `.descriptionSeed` `.loreRefs` | — |
| `engine/group.js` | CCODE-307: a group is a CAPABILITY SET rather than a pool of hitpoints — coverage (the union of what its members can do, binary per capability) and depth (how many supply each) are different numbers, so losing one of six spears costs depth and losing the only mender costs coverage entirely, and cohesion is a third stat saying how much of what a group HAS it can actually bring. | NONE YET — the model and its fidelity harness ship first. The player-facing surface is the roster reading 'RESTORE: only Sprig', which is the sole-coverage number this computes and the shape all nine authored downedEffects are already written in. | NONE YET — called by scripts/group_fidelity.mjs and tests/group_capability.mjs. Per SPEC_group_aggregation §5.5 the live wiring waits on the fidelity result, which currently says the aggregate destroys coverage the real fight preserves. | 3 | `skill_battle.js` `app.js` | **6** | `po/SPEC_group_aggregation.md` | — |
| `engine/intercept.js` | *— unstated —* | *—* | *—* | 10 | `battle_turn.js` `skill_battle.js` `app.js` | **6** | — | — |
| `engine/evolution.js` | Living gear — an item that wakes through stages as a companion bond and the item×companion co-use count cross thresholds; the stage the engine owns, never the model. | The LIVING GEAR block and the AN ITEM WAKES beat when a stage unlocks. | A stage's unlockBond and unlockCoUse both met through play — the engine advances the stage, the GM narrates the change. | 8 | `encounters.js` `gm_registry.js` `worldtick.js` `app.js` | **5** | — | — |
| `engine/skill_battle.js` | The two-sided contest — both wills declare a skill and intensity and roll through resolve.js; the higher net wins the round and shifts a bidirectional momentum meter, with energy attrition the second win condition. | The skill-battle receipt — momentum, each side's real math (the opponent's roll fogged by sense.js until earned), and the win. | Entering a structured skill duel (SNG-098); the recurrence engine's prestige challengers resolve through it. | 31 | `battle_turn.js` `encounters.js` `worldtick.js` `app.js` | **5** | — | — |
| `engine/canon.js` | Shared family canon — a personal-canon entity a player grew real EARNS its way into a shared store the whole world reads, contradictions RANKED by realness rather than rejected. Pure. | The SHARED WORLD CANON block — other players' promoted figures, places and threads the GM may reference as established. | A nominated entity's weight crossing the promotion threshold (the threshold IS the gate; no human curator). The sync orchestration that carries it across the repo lives in worldtick.js. | 18 | `feed.js` `worldtick.js` | **4** | `.provenance` | — |
| `engine/encounterFrame.js` | SNG-230: a pure LEGIBILITY layer over GM-narrated encounters — given a def + live state it yields the frame descriptor (KIND fight/chase/hazard/puzzle/standoff, the WIN condition, a 0–100 meter, the three always-named EXITS defeat/flee/fail) plus the pure rules for the finisher system (tier-gated one-beat COLLAPSE, WARD denial, KIT trivialization, and the fight→chase→fight chaining graph). No DOM, no engine calls. | The .enc-frame header above the encounter buttons — the kind icon/title, win condition, meter and the three exits — with the GM narration and freefield staying the real interaction. | An active encounter renders through frameModel (app.js) each round; onChoice/sbDeclare consult collapse/ward/trivialize; frameTransition drives fight→chase chaining. Content-driven via encounter_frame_content.json + encounter_frame_kinds.json (ward denials, challenge premises, per-kind copy). | 20 | `battle_turn.js` `encounters.js` `app.js` | **4** | `CONTENT.receiptLine` `.dangerLevel` | — |
| `engine/journey.js` | Routes over the mixed graph — roads weighted by real walking days, plus the gate network — returning two NAMED options rather than one optimal one, and saying so honestly when the world has only one way through. | The ways-there line the GM is handed when the player sets out: "2.2 days through the Echo River Crossing gate, or 53.3 days on foot by way of the Axis Gate". | A travel intent — the same beat that already forces a moveTo. The route is the traveller's own: only gates they have actually found, which on a real save is 2 of the world's 26. | 6 | `caravan.js` `app.js` | **4** | `.connections` | — |
| `engine/sync.js` | GitHub transport for the shared world, swappable by design — nothing outside this file knows the world lives on GitHub. Character/profile backup, ledger append, shared-scene primitives. | The save-sync status and the shared-world writes; localStorage stays authoritative and this is the optional layer on top. | A save/backup, a ledger append, or a shared-scene write in a co-op session. | 15 | `party.js` `worldtick.js` `app.js` | **4** | — | — |
| `engine/assignments.js` | Hold DELEGATED WORK as state so the world turns instead of narrating: a named person put in charge of ongoing work is an assignment the tick ADVANCES (progress/stall/problem/done), and a charge set against a crisis is the mechanism that can push the crisis back — the inversion of asking what a person merely felt. | The return-tick's news reports what MOVED on the work (a problem hit, a charge finished, a crisis EASED/HELD); the DELEGATED WORK block lets the GM speak to the commitments; the person's statusNote carries the colour. | delegateOps (the player puts a known person in charge of ongoing work) writes an assignment; runWorldTick advances them on return and lets progressAgainst a crisis counter its worsening. No delegation → no assignments, and an empty news block is legitimate. | 7 | `gm_registry.js` `worldtick.js` `app.js` | **3** | — | `delegateOps` |
| `engine/caravan.js` | Trade as a ROAD to prices the game already has, not a new economy: a load leaves a hold's store, travels a real route, can be robbed on the way, and is sold where it lands. The differential is authored — 8 raw material is 32 in the valley and 115 in the Gearlands. | News from the road while you are elsewhere: a load arriving and what it fetched, a caravan robbed, or people who did not walk on. The GM is told what is on the road and whether anyone is walking with it. | A holdingOps "caravan" op sends one; the world tick moves it, rolls the road's own danger, and sells it on arrival — it runs itself while you are not looking, the same promise a hold's store makes. | 9 | `gm_registry.js` `worldtick.js` `app.js` | **3** | `.dangerLevel` | — |
| `engine/encounters.js` | Typed multi-round structures — duels, challenges, puzzles — that CONSUME the d100 resolution and map receipts onto encounter state through data tables; the GM narrates the state, never advances it. | The ACTIVE ENCOUNTER receipt each round and the AVAILABLE ENCOUNTERS the GM may offer here. | A GM newEncounter/encounterOps, or firing one of the location's available encounters; each round consumes an ordinary resolve.js roll. | 21 | `battle_turn.js` `gm_registry.js` `app.js` | **3** | — | `encounterOps` `newEncounter` |
| `engine/latentarcs.js` | The world's OWN agenda: latent arcs foment on the world count whether or not the player has seen them, and surface at thresholds as first contact — so discovery is a late event in something that has been building, and the world is not hero-dependent. Every arc carries a cause that existed before it surfaced. | The return-tick surfaces an arc as a rumour now specific enough to repeat, or reports one that resolved itself without you; a surfaced arc rides in the STIRRING IN THE WORLD block for the GM to develop into a face, a changed place, or a thread. | runGenerationTurn (in worldtick.js) runs on the world count alongside the return-tick — seeds new arcs from the disposition of the regions the player knows, foments the existing ones, and surfaces those past the threshold. No known regions → nothing seeds; an arc may resolve or grow with no player involvement. | 9 | `gm_registry.js` `worldtick.js` `app.js` | **3** | — | — |
| `engine/newsvoice.js` | *— unstated —* | *—* | *—* | 10 | `worldtick.js` | **3** | `rules/news_templates.json` | — |
| `engine/seeking.js` | *— unstated —* | *—* | *—* | 7 | `worldtick.js` | **3** | `.wants` `.want` | — |
| `engine/corrections.js` | The game self-heals — a bounded stateOps the GM proposes and the engine validates and applies to the asking player's OWN save. REPAIR, not wish: fix data that is wrong, never grant what play did not earn. | The POSSIBLE ERROR advisory in the prompt and the Repair panel; the ✦ receipt when a correction lands. | An anomaly detected at assembly (detectAnomalies), or the player opening the Repair panel — the GM emits stateOps, the engine adjudicates. | 6 | `gm_registry.js` `app.js` | **2** | `.gender` `.pronouns` | — |
| `engine/facts.js` | A durable, non-scrolling ledger of load-bearing facts (a rescue, a death, a promise) — unlike the windowed chronicle, the whole active ledger is fed every turn as truth the GM must never contradict. | The ESTABLISHED FACTS block — persistent, authoritative truths regardless of how many scenes have passed. | A GM factUpdates op adds or resolves a fact; every turn the active ledger is carried into the prompt. | 4 | `gm_registry.js` `app.js` | **2** | — | `factUpdates` |
| `engine/gm.js` | Assembles the GM prompt and parses the structured reply — code owns the rules and the dice, the model owns the words. | All narration and the choice buttons; also the salvage path that keeps prose on screen when the reply parses badly. | Every turn — it assembles the prompt and parses the reply. The one module that fires on literally every beat. | 24 | `gm_registry.js` `app.js` | **2** | `rules/quest_structure.json` `.descriptionSeed` `.npcsPresent` `.encounterFlavor` | `scene` |
| `engine/incapacitation.js` | SNG-309: what happens when the PLAYER goes down — health<=0 is never instant death, it is an INCAPACITATION whose outcome (revived · spared · left_for_dead · slain) is decided by who put you there and who was with you, and a slain player enters the same death.js ladder as any figure rather than a terminal boolean. | The aftermath of any lost encounter: waking with your gear gone, a named companion bringing you round, or the death state itself — plus the roster line, which now says WHERE you are in the dark instead of "their story is over". | app.js calls incapacitationOutcome at the one place an encounter ends in "incapacitated"; a slain result goes through enterDeathState (NOT character.dead, which is the terminus and belongs only to a SEALED death), and worldtick deepenDeaths now includes the character so the clock reaches the player too. Warning of a lethal encounter is upstream, in encounters.js:lethalOfferClamp. | 8 | `battle_turn.js` `app.js` | **2** | — | — |
| `engine/narration_voice.js` | The narration's voice for table play — the place's earned register (concrete↔poetic) and the read-aloud constraint, plus a picker that prefers the good installed voices over the default worst one. | The NARRATIVE REGISTER and READ ALOUD blocks, the Settings read-aloud toggle, and the ▶ speak control's voice. | Always (the register colours every beat); the read-aloud directive when spoken play is on. | 7 | `gm_registry.js` `app.js` | **2** | — | — |
| `engine/party.js` | Shared-world play — scene publication, the open-scene index, and merge-safe writes so two players in one scene cannot clobber each other. | The party beat feed and the presence indicator when someone else is in your scene. | Publishing a scene beat in a shared session, and the presence indicator when another player is in your scene. | 38 | `gm_registry.js` `app.js` | **2** | `world/scenes/_open_index.json` | — |
| `engine/places.js` | Location permanence — durable changes, things left behind and discoveries that OUTLIVE the scene, fed back on every return visit. | The location panel's "what happened here" lines on returning somewhere you have been. | A GM placeUpdates op writes durable change; returning to a place reads it back. SNG-176 added recall keyed to the QUESTION, not only to where you stand. | 10 | `gm_registry.js` `app.js` | **2** | `local_layouts.json` `region_maps.json` `.parentId` `.want` | `placeUpdates` |
| `engine/presence.js` | Who is around today — the half of the world the GM was never shown. 117 authored figures exist and nothing offered them, because npcsPresent comes back IN the turn, KNOWN PEOPLE is only who the player has already met, and generateRequest is reactive by rule. | Strangers who turn up and are worth knowing: someone needing a hand, someone with news, someone owed — and, more rarely, a figure of real standing who has somewhere else to be. | Every turn, as ambient context. A per-person daily chance = their tier's rate x how near they are, with reach scaling by standing, so a market town is crowded and a remote post is quiet — and when nobody is near, it offers nobody. | 6 | `gm_registry.js` | **2** | `.homeLocation` | — |
| `engine/sense.js` | The graduated sense-of-success filter — the engine always knows the true odds; the character perceives only what their attunement earns, and an opponent's roll is fogged until sense is high enough. Pure. | The precision of the success-chance readout (a vague versus exact odds read) and the fog over an opponent's roll in a skill battle. | Any success preview and any skill-battle round — attunement gates how much of the true math is shown. | 6 | `gambit.js` `app.js` | **2** | — | — |
| `engine/toolkit.js` | The 'what you could reach for' summary — a compact, recency-weighted hold-up of the player's OWN crafts, combines, aspiration, carried items and function-gap, so the GM can offer ONE, lightly. | The TOOLKIT block — options the player may have forgotten, surfaced as a light offer (rule 16B), never a menu. | Assembled each turn; SNG-173 made its quiet-craft pool recency-based so long-unused crafts fade rather than crowd. | 1 | `gm_registry.js` `app.js` | **2** | `.knowledge` | — |
| `engine/worldtick.js` | The world moves while you are away — on re-entry after in-game days pass, active events advance on their own clocks, delegated work progresses and arcs ferment, so a crisis ignored worsens and time away is time the world used. | The away-digest news on return — what advanced, what a delegated crew did, what stirred in the world. | Re-entering play after in-game days have passed (runWorldTick plus the SNG-191 generation turn on the world count). | 66 | `gm_registry.js` `app.js` | **2** | `world/regions/valley.json` `world/arcs/valley.json` `tradition_epics.json` `SYSTEM_SPEC.md` | — |
| `engine/affinities.js` | How a place shapes a roll, both ways and both legible — a TYPE affinity (a tag's flat bump to matching skills) and a VECTOR alignment (an ability whose axes agree with the place's strong spectrum). | The affinity receipt line — why THIS place helped or resisted the action. | An action at a place — the flat and vector modifiers are computed and surfaced on resolve. | 5 | `app.js` | **1** | — | — |
| `engine/arceffects.js` | SNG-273: turns a greater arc's current stage into things the world DOES — what a craft costs, what the roads cost, what you are likely to meet, how people are carrying themselves. | The energy line at the point of use ("12 energy (+4, The Poles Pull)"), the effects listed under each arc on The World tab, and the mood of NPCs in play. | A greater arc sitting at a stage that carries effects; read fresh on every cost, every travel and every GM turn. | 8 | `app.js` | **1** | — | — |
| `engine/authormode.js` | SNG-207b: the AUTHOR god-mode — Erik-as-author (not the character, not the in-fiction GM) sets anything on the save with NO fairness or trace check, via a DELIBERATELY SEPARATE entry point (applyAuthorOps), never a skipFairness flag on the fair-GM stateOps. It crosses fairness (grants xp/levels/power items/abilities, forces arc stages) but never safety — rating/minor are not state, so they are simply never exposed. | A dev-gated '⚙ Author' panel (next to 🔬 Machine): set XP/level/skill-points, restore or raise vitals, grant an ability or a power item, force any world-arc stage. Every edit shows in a Recent-author-edits log. Invisible outside dev mode. | renderAuthorPanel's controls call applyAuthorOps with one op each (guarded by AUTHOR_OPS), then saveCharacter + re-render. Every op appends to character.authorEdits (append-only, separate from the fair-GM corrections ledger) so even god-mode leaves an audit trail. | 2 | `app.js` | **1** | `.parentId` | — |
| `engine/backfill.js` | One-time retroactive credit for characters made before engine-XP, bonds and the practice ledger existed — a principled, CAPPED reconstruction from each character's own durable record, never an exact replay. | The one-time credit a pre-feature character receives on load (XP, bonds, practice), shown as its receipts. | Loading a character created before a feature existed, once — the same versioned, idempotent discipline reconcile.js generalizes. | 5 | `app.js` | **1** | — | — |
| `engine/battle_turn.js` | THE ONE PATH a skill battle takes, in play and in a harness — the menu, the declaration, the rank, the guards, sense → action → bonus, the apply to the character, and the end (XP, the incapacitation table). app.js delegates to it; tests/lib/realgame.mjs drives it. | Every fight turn the panel resolves; the aftermath of a knockout (gear taken, days down, death) — which a skill battle never reached before 2026-09-05. | A player's violence against a target (duelFromTarget), each resolved step of a fight turn (playTurn), the end of a contest (endBattle). | 12 | `app.js` | **1** | `.dangerLevel` | — |
| `engine/battleprompt.js` | *— unstated —* | *—* | *—* | 4 | `app.js` | **1** | `.appearance` | — |
| `engine/chronicle.js` | The Chronicle reads the accreted self back to the player — one assembled page that WITNESSES who the character has become, from deeds, bonds, standing and the grown arc. | The Chronicle screen and the CHRONICLE block — the character's story so far, plus a one-paragraph 'story so far' synthesis. | A scene closing appends a line; opening the Chronicle assembles the page (app.js owns the async synthesis call). | 10 | `app.js` | **1** | `.provenance` | — |
| `engine/coliseum.js` | *— unstated —* | *—* | *—* | 6 | `app.js` | **1** | — | — |
| `engine/devcapture.js` | See the machine: a dev-only ring buffer holding the last two-dozen model exchanges — assembled prompt, raw response, parsed result, ops fired — so the by-hand SNG-179 diagnosis (read the prompt, read the raw output, see what the engine did) is a standing instrument, not a devtools dig. | None for a player — it is dev-mode-only (the 🔬 Machine screen behind isDevMode()). Its value is that a defect that used to cost a live play session to find is now visible in a panel. | callClaude invokes the observer app.js registers ONLY under isDevMode() at boot; every model call (GM turn + intent-parse + narrate + generate) routes through that one seam. In a player view the observer is null and nothing is recorded (§3.4). | 7 | `app.js` | **1** | — | — |
| `engine/entityDetail.js` | ONE formatter for the hover/tap detail of the three things that recur everywhere — a skill, an NPC name, an item — so the detail reads identically at every render site. Pure. | The detail card that appears wherever a skill, name or item is shown — the same shape everywhere. | Opening any entity's detail — the app gathers the live values and this formats them. | 4 | `app.js` | **1** | — | — |
| `engine/feed.js` | SNG-168: the WORLD FEED — a family scrapbook of turns a player DELIBERATELY chose to share (narration + image + world-date + poster rating), appended merge-safe to one shared world/feed.json and rating-lensed on read. Explicitly NOT canon: a post never hydrates into another player’s CONTENT (that is the separate shared-canon path). | The per-turn 📮 Post control and the 📮 Feed screen — other family members’ shared moments, reverse-chron, softened or image-withheld above the viewer’s content ceiling. | Posting a turn (buildFeedPost → appendFeedPost via pushMergedFile — the click+confirm IS the per-post consent) and opening the feed (fetchRepoJSON → feedForViewer lenses it). Sync-gated; no PAT → no post. | 4 | `app.js` | **1** | `world/feed.json` | — |
| `engine/gambit.js` | Declared multi-step plans — the player lays out a sequence of intended actions with fallbacks, sees what their experience reveals of each step's odds, and executes; steps resolve in order through the same deterministic resolver. | The gambit builder (goal, steps, fallbacks) and the step-by-step run receipts. | The player asking to plan — a gambitOps op opens the builder pre-filled; a failed step blocks the run per its fallback. | 6 | `app.js` | **1** | — | — |
| `engine/gm_registry.js` | The declared table of everything the GM can be told — one row per context block, iterated at every call site so a capability cannot be built and left unreachable. | NONE. It is the §23 wiring contract itself; the player meets it only as a GM that knows things. | Every GM call — assembleGMContext iterates it at all four call sites. NONE player-facing; the player meets it only as a GM that knows things. SNG-182 also resolves name tokens here. | 3 | `app.js` | **1** | `CONTENT.region` `CONTENT.events` `CONTENT.lore` `CONTENT.traditionMotivations` | — |
| `engine/imageprompt.js` | *— unstated —* | *—* | *—* | 3 | `app.js` | **1** | — | — |
| `engine/intensity.js` | The three intensities a craft resolves at — Conserve / Standard / Surge — scaling energy and effect, with backlash risk carried by Surge alone; AUTO picks the minimum the task needs and never auto-surges. | The intensity choice on an ability action and the backlash receipt when a surge costs. | An ability action with an intensity setting — surge is always the player's deliberate choice. | 7 | `app.js` | **1** | — | — |
| `engine/intent.js` | Intent confirmation for costly acts — nothing irreversible or world-scale commits until the player confirms, the same shape character creation already uses. | The confirmation card before a harmful or departing act, naming the cost in plain language. | A costly or irreversible act reaching resolution — the confirmation card is what makes it fire, and it commits nothing until the player says yes. | 12 | `app.js` | **1** | `.connections` | `offerIntent` |
| `engine/pacing.js` | Scene rhythm — the world PUSHES when a beat goes quiet (pressure, escalating and place-respecting) and OFFERS when there is room (SNG-194), so the player never has to ask the world to be interesting. Pure. | The woven THE WORLD ACTS pressure beat and the THERE IS ROOM offer cue — both in-fiction, never a system announcement. | Quiet turns crossing a threshold (pressure); roomForAnOffer computing a positive opening off cooldown (offer). | 8 | `app.js` | **1** | — | — |
| `engine/pressure.js` | SNG-245 the PRESSURE QUEUE — a registry of driven things that WANT to happen to the player (a bonded NPC's unmet want reaching out, a threat that comes to your ground), fed by producers from the agendas already in play, that SNG-080's quiet-turn trigger PULLS FROM so the world acts with a real, aimed, specific thing rather than a generic invention. Pure. | The woven THE WORLD ACTS beat now carries a DRIVEN thing — a named person at the door, a beast that becomes a framed defend-encounter — not a generic 'something happens'. | Producers run on the world tick (npc-want: a bonded, long-unseen NPC with an authored want; threat-attack: a real beast from the place's eligible pool, rolled on danger × the aggression pref); the quiet-turn trigger (maybeWorldPressure) pulls the top entry aimed at where the player stands. | 8 | `app.js` | **1** | — | — |
| `engine/projects.js` | *— unstated —* | *—* | *—* | 10 | `app.js` | **1** | — | `projectOps` |
| `engine/recipes.js` | SNG-201: found once, known forever — a braid pairing anyone has already found becomes a global RECIPE; a later finder ADOPTS its name/prose/emergent reach instead of minting a duplicate. The discovery stays theirs (bond, moment, rank-1); the craft is the world's, with first-finder attribution. A NEW store (not emergence_recipes, which is prescriptive) that stays DESCRIPTIVE — never a gate (the SNG-196 regression). Numbers are never shared: tier/levelReq/energy derive from each adopter. | When you braid a pairing someone found first, a RECOGNITION moment ('first found by <name> — and now yours too') instead of a creation one; the world learns the crafts you find first, under your name. | A ripened pairing already present in the cached recipe store (adopt at mint, no authoring call); syncBraidRecipes publishes first-finder braids + adopts any landed meanwhile, riding pushMergedFile (first-PUT-wins, SHA-retry). A stub never promotes. | 8 | `app.js` | **1** | `combination_recipes.json` | — |
| `engine/roundreceipt.js` | The ROUND RECEIPT — the line the player reads and the GM narrates from. Extracted from app.js so it could be TESTED: roundVerdict (did the meter actually move, with a 0.5 deadband), interactionClause (how two declared moves read against each other), and receiptLine (the whole thing). | The per-round receipt in the encounter ribbon, and — because the GM narrates FROM these beats — the prose of every fight turn. | Every resolved contest round (app.js sbRoundReceipt / sbFightBeat), and tests/contest_sim.mjs, which Monte-Carlos it across threat bands to assert a decisive round is never reported as even. It lives here BECAUSE it shipped a permanent "neither gains — it's even" while it was untestable inside app.js. | 6 | `app.js` | **1** | — | — |
| `engine/vectors.js` | Makes a location's spectrum character legible to the PLAYER — a place's strong axes are felt after a visit, subtler axes need attunement or a perceiving ability, and precursor address_sense reveals all. | The place's felt axes shown to the player (the GM already has the full vector); SNG-011 progressive legibility. | Visiting a place, using a perceiving ability there, or precursor address_sense — each reveals more of the vector. | 4 | `app.js` | **1** | — | — |
| `engine/wheelgeom.js` | SNG-202: place a craft on the great circle by its COMPOSITION. The angular authority is the ONE circle the world already carries — a tradition's ring position — so nobody invents a second coordinate system. The headline two-point case: a braid sits at the shorter-arc midpoint of its two parents, radius pulled inward by their separation (adjacent parents hug the rim; a cross-circle braid sinks to the centre). The antipodal case is deterministic (clockwise from the lower ring) and flagged 'spans the circle'. | On the skill wheel, a braid is drawn BETWEEN the two axes it braids — not on one parent's spoke — so you can see your build's shape; the deep cross-circle braids sit toward the centre, the antipodal ones read as spanning the wheel. | buildWheelModel calls braidPlacement for every minted braid (a customAbility with minted.from of two parents) to compute its (angle, radius) from the parents' ring positions. Pure + deterministic — no force layout, same composition same position every load. | 10 | `app.js` | **1** | — | — |
| `engine/whois.js` | SNG-299: answers 'who or what is that?' for any name the world has a record of — figures, arcs, titles and tiers — and says where the fuller account lives. | Any known name in any rendered prose is underlined and clickable; the popup gives what the world knows, with a codex button when a page exists. | Clicking a linkified name; the index is rebuilt on every render from the roster, the arcs, the player's codex and the titles actually earned. | 3 | `app.js` | **1** | `.wants` `.appearance` `.gender` `.pronouns` | — |
| `engine/worldglobe.js` | *— unstated —* | *—* | *—* | 33 | `app.js` | **1** | `.connections` `.worldPos` `.worldPosInherited` | — |
| `engine/worldtab.js` | Builds THE WORLD tab — who is doing what to your arcs — as a pure function of world state, so the render can be EXECUTED in a test instead of pattern-matched against its own source. | The World tab on the character sheet: each arc by stage NAME, who is pushing it and who is holding it back (names you have met marked apart), what it cost in casualties and strikes, who is being reached for in the dark, and who has not been home. | Opening the character sheet and choosing The World; fed by arcPeopleView + worldPeopleFooter off the offscreen world tick. | 1 | `app.js` | **1** | — | — |

## GM verbs handled inside `app.js` itself

32 of the reply contract's ops never reach an engine module — `applyTurn` handles them inline. Some of those are correct (a narration flag has nowhere else to live); some are engine logic sitting in the view layer, which is where it gets hard to test.

`_applyFailedOp` · `_appraisal` · `_engineClosedScene` · `_retold` · `arcOps` · `bonusEarned` · `characterDeltas` · `discovery` · `exchangeOps` · `gambitApt` · `gambitOps` · `generateRequest` · `ledgerEvents` · `markTeacher` · `momentArt` · `offerAcquisition` · `phase` · `sceneEnded` · `sel` · `senseBlinded` · `senseDone` · `senseLine` · `setupBonus` · `standingOps` · `stateOps` · `text` · `timeAdvanceHours` · `timeOps` · `unlockLivingCurrent` · `unlockPrecursor` · `unlockSubstrate` · `unlockWildCurrent`

## Blast radius — read this before changing a module

`reach` is the TRANSITIVE count: every module that can be affected by a change here, not just direct importers. A spec touching one of these should state the number before work starts.

- `engine/namematch.js` — **47** modules downstream, serves `offer`
- `engine/skilltree.js` — **41** modules downstream
- `engine/ladder.js` — **36** modules downstream
- `engine/traditions.js` — **35** modules downstream
- `engine/earnedpower.js` — **30** modules downstream
- `engine/recurrence.js` — **30** modules downstream
- `engine/inventory.js` — **29** modules downstream, serves `deriveItem` `itemUpdates`
- `engine/reputation.js` — **29** modules downstream, serves `deeds`

## The six lenses (SNG-183) — the defect shapes this batch produced, and where each is caught

Not one of this batch's twelve built-never-reached, three never-fired op-classes, or the lore-blind loader was a *coding* error. Each was a missing **connection** — a value duplicated where nobody updates it, or a chain with a broken link. These are the shapes, stated so they are checked rather than remembered.

| lens | the shape | caught by |
|---|---|---|
| **L1** built-never-reached | an export with no non-test caller | `testOnlyExports` + `importedNeverCalled` ratchets (wiring_audit) |
| **L2** permission isn't initiative | a reachable capability with no *trigger* | this map's **what makes it fire** column — `NONE` on a module with a surface |
| **L3** a guard whose result is not read | `npm test \| grep … && commit` chains on grep's status | discipline: every ship verifies by explicit exit code, no test result across a pipe |
| **L4** orphaned content | a content file no consumer references | loreRef resolution gate (content_ci); `content it reads` column here |
| **L5** never-fired ops | an op with all its wiring that still does nothing | dispatch check (wiring_audit) + the runtime `_opLedger` on the feedback report |
| **L6** a universal gate for a local fact | a global floor encoding a local condition | stranded-encounter check (content_ci): `minDanger` past every tag-home's `dangerLevel` |

L3 is the one that cannot be caught by more verification — it *is* the verification layer — so it lives as a rule, not a gate. The rest run in `npm test`.

