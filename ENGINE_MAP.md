# ENGINE MAP

> **Generated** by `scripts/engine_map.mjs` — re-run it rather than editing this file.
> Mechanical columns are derived from the static import graph, which is complete: `app.js` contains **zero dynamic imports**.
> **`purpose`, `player-visible surface` and `what makes it fire` are AUTHORED** in `scripts/engine_map.authored.json` and are never overwritten by a regeneration.

**59 modules · 59/59 described.** Two flags carry the defect shapes this map exists to catch. **`player-visible surface: NONE`** (L1) is how a capability gets built, tested, and never met. **`what makes it fire: NONE` on a module that DOES have a surface** (L2, "permission isn't initiative") is how a capability the player can reach still never happens — the teacher gate that never fired.

**How each derived column is measured** — so the columns can be trusted or corrected rather than believed:

- **depends on / depended on by** — static `import` statements. Complete: `app.js` has zero dynamic imports.
- **reach** — transitive closure of *depended on by*. `app.js` counts once if reachable at all, directly or through a chain.
- **content it reads** — literal `*.json`/`*.md` paths, `CONTENT.*` keys, and content-schema fields harvested from the real location and NPC corpus. **Excluded:** field names that collide with JS members (`map`, `name`, `id`, …) and fields ≥9 modules read, which discriminate nothing. 36 fields qualify.
- **GM verbs** — from `applyTurn`'s dispatch in `app.js`, *not* from imports: each `turn.<verb>` handler block is walked and the verb attributed to whichever module owns the functions called inside it.

| module | purpose | player surface | what makes it fire | exports | depended on by | reach | content it reads | GM verbs |
|---|---|---|---|---|---|---|---|---|
| `engine/namematch.js` | The one name-resolution primitive — slugify and fuzzy-match an entity the same way everywhere, so codex, quests and inventory can never disagree about what the GM just named. | Indirect but constant: every id the player sees, and the quest panel's "couldn't match" note when the GM names something that does not resolve. | Always — every entity the GM names is resolved through it on the turn it is mentioned. | 7 | `affiliation.js` `assignments.js` `canon.js` `codex.js` `corrections.js` `encounters.js` `facts.js` `generate.js` `gm.js` `inventory.js` `latentarcs.js` `npcs.js` `party.js` `personalArc.js` `places.js` `practice.js` `progression.js` `quests.js` `reconcile.js` `worldtick.js` `app.js` | **29** | — | `offer` |
| `engine/quests.js` | Quests as typed state the GM drives through clamped ops — the model proposes, the engine adjudicates and refuses in the open. | The quest panel: active quests, stage progress lines (✦ …), the resolution choice when every stage is done, and refusal notes when the GM names the wrong stage. | A GM stageOps/questUpdates op, or the quest-log 'Take it on' button. SNG-162 gave it a play-time trigger; before that a structured quest could only be closed from the fix button. | 15 | `codex.js` `generate.js` `gm_registry.js` `npcs.js` `progression.js` `reconcile.js` `app.js` | **19** | `.arcId` `.boundToCharacter` | `questUpdates` `stageOps` |
| `engine/playerprofile.js` | Player identity across characters, and the earned play-style (tendencies, aptitudes) that accrues on each character from how it is actually played. | The profile screen's play-style readout, and the character-select roster. | Every resolved action accrues play-style; the profile screen and character-select read it. | 19 | `art.js` `canon.js` `app.js` | **15** | — | — |
| `engine/art.js` | Images for characters, locations, items, NPCs and big moments, from either a static file or a generated prompt, behind one seam. | Every image in the app: location art, portraits, item art, the moment gallery. | Any record without an image, at first render or on a bond crossing (ensureBondPortraits) — never eagerly, so a passing face costs nothing. | 20 | `corrections.js` `npcs.js` `app.js` | **13** | `.descriptionSeed` `.encounterFlavor` `.voiceHints` `.appearance` | `imagePrompt` |
| `engine/traditions.js` | The Great Circle geometry — the 24-station ring, neighbours, antipodes and distances — read from content, never hardcoded. | The skill wheel's ring layout and the tradition labels shown on abilities and standing. | Always — the ring geometry is read whenever an ability, domain or standing is shown. | 14 | `functions.js` `progression.js` `reconcile.js` `standing.js` `state.js` `app.js` | **13** | — | — |
| `engine/npcs.js` | NPC permanence — everyone met, authored or invented mid-scene, gets a durable registry entry the GM writes to through clamped npcUpdates. | The people list and each NPC's detail card: who they are, where you met, what has passed between you. | A GM npcUpdates op writes the registry; the people list and detail card read it. NB (SNG-179): the mark-a-relationship ops fire far less than the list-shaped ones. | 21 | `companions.js` `company.js` `corrections.js` `entityDetail.js` `gm_registry.js` `worldtick.js` `app.js` | **12** | `.communityId` `.questSeeds` `.homeLocation` `.wants` | `npcUpdates` `relationshipDeltas` |
| `engine/reputation.js` | Deeds are the source of truth and reputation is a VIEW over them — a community's opinion is the sum of the deeds it knows about. | The standing list on the character screen (band per people). Empty for most players today — the gap BATCH-12 §3 exists to close. | A deed is recorded (GM deeds op or an encounter), and the standing list reads the view. Deeds fire heavily; the mark-ops that would move people-standing do not (SNG-179). | 5 | `gm.js` `progression.js` `standing.js` `app.js` | **11** | `.communityId` | `deeds` |
| `engine/skilltree.js` | Skill legibility and gating: tiers, attribute gates, and what is learnable now versus later. | The skill wheel and skill graph — tier rings, locked/unlocked state, and the reason a locked ability is locked. | Opening the skill wheel or graph; a learn/deepen action re-reads it in place. | 22 | `gm_registry.js` `intensity.js` `progression.js` `app.js` | **9** | — | — |
| `engine/affiliation.js` | The one answer to 'what kind of being is this person, and what do they practise' — read from their own role, skills and region with provenance, so the two mint paths can never disagree about it again. | Indirect: every teacher who can now teach and every people who can now credit you — a met NPC's craft becoming legible is what lets standing move and a mentor mentor. | Meeting anyone in play (npcs.js applyNpcUpdates), minting anyone (generate.js), and the reconcile v11 backfill over people already in the save. Before SNG-185 the meet-path stamped nothing, which is why registry-only people had no craft. | 5 | `generate.js` `reconcile.js` `app.js` | **8** | — | — |
| `engine/company.js` | The unified company: companion, trainer, liaison, partner and ally are stacking ROLES a person in your party holds, each wired to the system that already implements it. | The company panel — who travels with you and what each one is to you. | Recruiting a met NPC, and — SNG-175 — a bonded teacher's next-step offer now surfaces through teachersForGM. The gate existed for levels; the initiative did not until then. | 13 | `gm_registry.js` `progression.js` `app.js` | **8** | `.teaches` `.curriculum` `.liaisonFor` | — |
| `engine/progression.js` | Character growth with numbers you can point at — sub-attributes, levelling, ability acquisition and the receipts for each. | Level-up prompts, the attribute allocation screen, new-ability offers, and the ✦ receipt lines when something is earned. | A resolved action that crosses a threshold, a level-up, or a GM offerAcquisition/offerPromotion — the player commits on their own screen (Law 9). | 34 | `backfill.js` `corrections.js` `gm.js` `gm_registry.js` `practice.js` `app.js` | **7** | — | `markDefiningMoment` `newAbility` `offerPromotion` |
| `engine/genschema.js` | A dependency-free JSON-Schema checker and repair helper for the generative pipeline — exactly the subset the derived npc/location/arc schemas use, so a grown entity is shaped like an authored one. | NONE directly — it validates generation; the player meets it only as generated content that never arrives malformed. | A generateRequest — generate.js validates (and auto-repairs) the model's output against the derived schema before minting. | 3 | `generate.js` | **6** | — | — |
| `engine/resolve.js` | Deterministic d100 action resolution — the model never rolls; this turns an action and its modifiers into a rolled degree of success the whole engine reads. Pure. | Indirect but constant: the success-chance readout before acting and the outcome degree the narration is handed. No panel of its own — it is the dice under everything. | Every action that resolves — and every encounter, gambit and skill-battle round routes its roll through here. | 4 | `gambit.js` `sense.js` `skill_battle.js` `app.js` | **6** | — | — |
| `engine/claude.js` | Anthropic API transport, with MODEL_MAP as the single source of truth for task→model routing and a token/stop_reason log per call. | NONE directly — but every latency the player feels and every API error card comes from here. | Every callClaude — the play loop, ask-the-GM, generation, world-tick. NONE player-facing directly; it is the transport. | 6 | `gambit.js` `gm.js` `worldtick.js` `app.js` | **5** | — | — |
| `engine/codex.js` | The character's knowledge graph — what they have LEARNED as linked, typed topics (mysteries, factions, lore, people, places) with accumulating facts, written only through clamped ops. | The codex screen and the CODEX block, which lets the GM have the character ACT on what they know instead of re-explaining it. | A GM codexUpdates op writes a topic or fact; auto-consolidation (SNG-153) folds duplicates past a threshold; the codex screen reads it. | 15 | `gm_registry.js` `reconcile.js` `worldtick.js` `app.js` | **5** | — | `codexUpdates` |
| `engine/generate.js` | The ONE generative path — authors a new NPC, location or arc in-grain for its locale, validates against the derived schema, resolves before minting so it never duplicates, and persists it. | Any place or person that did not exist until the fiction needed it; the player meets these as ordinary world content. | The fiction naming a person or place that does not exist yet — a GM generateRequest, or the mint-on-move path. Never eager. | 29 | `canon.js` `chronicle.js` `gm_registry.js` `worldtick.js` `app.js` | **5** | `.communityId` `.connections` `.descriptionSeed` `.loreRefs` | — |
| `engine/inventory.js` | Items as first-class objects the character can carry and USE — normalized losslessly from legacy string saves, with quantities, uses and dedupe. | The inventory panel and the INVENTORY block; the receipt when an item is used or consumed. | A GM itemUpdates op writes the pack; using an item in a scene reads and decrements it. | 17 | `entityDetail.js` `gm_registry.js` `reconcile.js` `app.js` | **5** | — | `itemUpdates` |
| `engine/worldmap.js` | The data-driven map foundation: stable auto-positioning for every location including ones minted mid-play, plus the three zoom tiers. | The map screen and its World / Region / Location tier bar. | Opening the map (any of three tiers), and SNG-180 wired walkingDays to the place card so the world scale is met on travel. | 16 | `substrate.js` `app.js` | **5** | `.connections` `.poleIntensity` `.worldPos` `.waygate` | — |
| `engine/personalArc.js` | Every character's PRIMARY arc, generated from the backstory they wrote — the bound-arc shape SNG-132's runtime surfaces and paces, so a player's own throughline is a real quest that is never foreclosed. | The personal-arc quest in STRUCTURED QUESTS, surfaced and paced like any bound arc. | Character creation generates it from the bio; a beat that touches it advances it; reconcile backfills it onto older saves. | 9 | `reconcile.js` `app.js` | **4** | `.disposition` | — |
| `engine/practice.js` | Competency as the residue of attention — an engine-owned ledger of ability use and co-activation that unlocks ranks at zero skill-point cost. | Aspiration progress and the unlock notices when practice crosses a threshold. | Every ability use records to the ledger; an aspiration or rank threshold crossing surfaces the unlock. SNG-173 made the toolkit's quiet-craft pool recency-based so it stops draining. | 15 | `backfill.js` `evolution.js` `gm_registry.js` `app.js` | **4** | — | — |
| `engine/standing.js` | How a people regards you — seeded at creation from who you are, drifting with the company you keep, and moved by narrated acts the engine adjudicates rather than the model. | The standing list on the Chronicle (band per people and settlement), the ✦ receipt when a band changes, and the GM's own knowledge of your welcome. | Seeded at character creation; drips per in-game day from the company you keep; a GM standingOps op moves it from a narrated act. BATCH-12 §3 — before it, only a structured quest with a people-effect ever wrote peopleDisposition, which is why Erik's standing screen sat empty. | 11 | `gm_registry.js` `reconcile.js` `app.js` | **4** | `.teaches` `.liaisonFor` | — |
| `engine/substrate.js` | The SECOND difficulty map — the nanite lattice varies in density by place and each tradition is tuned to a band of it, so where you stand changes what you can do. | The success-chance readout before acting (the penalty is folded in, per SNG-116) and the thin/crowded lattice note the GM works into prose. No named receipt of its own. | An ABILITY action at a place (weapon actions are substrate-free, SNG-089). The success-chance readout and the thin/crowded note are the surfaces; SNG-183 note — the geographic FIELD is precomputed at load but the ruler question is open. | 15 | `gm_registry.js` `reconcile.js` `state.js` `app.js` | **4** | `CONTENT.schools` `.substrateSource` | `adoptSchool` |
| `engine/sync.js` | GitHub transport for the shared world, swappable by design — nothing outside this file knows the world lives on GitHub. Character/profile backup, ledger append, shared-scene primitives. | The save-sync status and the shared-world writes; localStorage stays authoritative and this is the optional layer on top. | A save/backup, a ledger append, or a shared-scene write in a co-op session. | 14 | `party.js` `worldtick.js` `app.js` | **4** | — | — |
| `engine/assignments.js` | Hold DELEGATED WORK as state so the world turns instead of narrating: a named person put in charge of ongoing work is an assignment the tick ADVANCES (progress/stall/problem/done), and a charge set against a crisis is the mechanism that can push the crisis back — the inversion of asking what a person merely felt. | The return-tick's news reports what MOVED on the work (a problem hit, a charge finished, a crisis EASED/HELD); the DELEGATED WORK block lets the GM speak to the commitments; the person's statusNote carries the colour. | delegateOps (the player puts a known person in charge of ongoing work) writes an assignment; runWorldTick advances them on return and lets progressAgainst a crisis counter its worsening. No delegation → no assignments, and an empty news block is legitimate. | 5 | `gm_registry.js` `worldtick.js` `app.js` | **3** | — | `delegateOps` |
| `engine/canon.js` | Shared family canon — a personal-canon entity a player grew real EARNS its way into a shared store the whole world reads, contradictions RANKED by realness rather than rejected. Pure. | The SHARED WORLD CANON block — other players' promoted figures, places and threads the GM may reference as established. | A nominated entity's weight crossing the promotion threshold (the threshold IS the gate; no human curator). The sync orchestration that carries it across the repo lives in worldtick.js. | 17 | `worldtick.js` | **3** | — | — |
| `engine/companions.js` | Traveling companions — present in every scene, voiced by the GM, mechanically helpful where their nature fits; the character carries only the ids, definitions are content. | The COMPANIONS block and the companion detail card — who travels with you and what they can do. | Always, for a present companion; an evolution threshold deepens one (SNG-175). | 7 | `backfill.js` `gm_registry.js` `app.js` | **3** | `.voiceHints` `.knowledge` `.appearance` `.hooks` | — |
| `engine/functions.js` | The function-family axis over the skill system — inverts the authored verb→family vocabulary (8 families, 24 verbs), measures a character's coverage, and recommends skills that fill the gap. | The function-gap line in the toolkit block, and the coverage read where a kit's missing family is named. | Assembling the toolkit (the gap), and any place the engine reasons about what KIND of act an ability performs. | 11 | `gm_registry.js` `toolkit.js` `app.js` | **3** | `function_vocabulary.json` | — |
| `engine/latentarcs.js` | The world's OWN agenda: latent arcs foment on the world count whether or not the player has seen them, and surface at thresholds as first contact — so discovery is a late event in something that has been building, and the world is not hero-dependent. Every arc carries a cause that existed before it surfaced. | The return-tick surfaces an arc as a rumour now specific enough to repeat, or reports one that resolved itself without you; a surfaced arc rides in the STIRRING IN THE WORLD block for the GM to develop into a face, a changed place, or a thread. | runGenerationTurn (in worldtick.js) runs on the world count alongside the return-tick — seeds new arcs from the disposition of the regions the player knows, foments the existing ones, and surfaces those past the threshold. No known regions → nothing seeds; an arc may resolve or grow with no player involvement. | 9 | `gm_registry.js` `worldtick.js` `app.js` | **3** | — | — |
| `engine/legends.js` | The world's great figures — heroes witnessed and villains scaling from riffraff to arc, so a world feels large when greatness passes through it. The engine detects the beat and GOVERNS the rarity; the GM narrates. | The LEGEND block — a great figure surfacing in the fiction, governed so it stays rare. | A qualifying beat (maybeLegendDetail governs the rarity); rides the generate + weight/recurrence + codex machinery. | 7 | `state.js` `app.js` | **3** | — | — |
| `engine/reconcile.js` | Save and content migration — versioned, idempotent, one-time steps that bring a character or content authored by an older engine up to current, so nobody loses a save to a schema change. | The one-time '✦ …' notices on load when a migration grants a character what a newer feature owes it. | Loading a save (or content) whose reconcile-version trails the engine — each step runs once, in order, then never again. | 5 | `state.js` `app.js` | **3** | `.connections` `.poleIntensity` `.homeLocation` | — |
| `engine/skill_battle.js` | The two-sided contest — both wills declare a skill and intensity and roll through resolve.js; the higher net wins the round and shifts a bidirectional momentum meter, with energy attrition the second win condition. | The skill-battle receipt — momentum, each side's real math (the opponent's roll fogged by sense.js until earned), and the win. | Entering a structured skill duel (SNG-098); the recurrence engine's prestige challengers resolve through it. | 4 | `encounters.js` `app.js` | **3** | — | — |
| `engine/worldtime.js` | The two clocks — character time (days and season, the player's own) and the world Kept Count (a shared, monotonic mark spoken in the local people's idiom), in different units so neither invents a date for the other. | The CURRENT TIME narration and the count rendered in the local idiom; the story/real time-mode setting. | Always — readClock runs every turn and the count advances about once per real hour; SNG-191 removed the invented day-number. | 16 | `gm_registry.js` `worldtick.js` `app.js` | **3** | `CONTENT.worldClock` | — |
| `engine/corrections.js` | The game self-heals — a bounded stateOps the GM proposes and the engine validates and applies to the asking player's OWN save. REPAIR, not wish: fix data that is wrong, never grant what play did not earn. | The POSSIBLE ERROR advisory in the prompt and the Repair panel; the ✦ receipt when a correction lands. | An anomaly detected at assembly (detectAnomalies), or the player opening the Repair panel — the GM emits stateOps, the engine adjudicates. | 5 | `gm_registry.js` `app.js` | **2** | `.pronouns` | — |
| `engine/encounters.js` | Typed multi-round structures — duels, challenges, puzzles — that CONSUME the d100 resolution and map receipts onto encounter state through data tables; the GM narrates the state, never advances it. | The ACTIVE ENCOUNTER receipt each round and the AVAILABLE ENCOUNTERS the GM may offer here. | A GM newEncounter/encounterOps, or firing one of the location's available encounters; each round consumes an ordinary resolve.js roll. | 14 | `gm_registry.js` `app.js` | **2** | — | `encounterOps` `newEncounter` |
| `engine/evolution.js` | Living gear — an item that wakes through stages as a companion bond and the item×companion co-use count cross thresholds; the stage the engine owns, never the model. | The LIVING GEAR block and the AN ITEM WAKES beat when a stage unlocks. | A stage's unlockBond and unlockCoUse both met through play — the engine advances the stage, the GM narrates the change. | 8 | `gm_registry.js` `app.js` | **2** | — | — |
| `engine/facts.js` | A durable, non-scrolling ledger of load-bearing facts (a rescue, a death, a promise) — unlike the windowed chronicle, the whole active ledger is fed every turn as truth the GM must never contradict. | The ESTABLISHED FACTS block — persistent, authoritative truths regardless of how many scenes have passed. | A GM factUpdates op adds or resolves a fact; every turn the active ledger is carried into the prompt. | 3 | `gm_registry.js` `app.js` | **2** | — | `factUpdates` |
| `engine/gm.js` | Assembles the GM prompt and parses the structured reply — code owns the rules and the dice, the model owns the words. | All narration and the choice buttons; also the salvage path that keeps prose on screen when the reply parses badly. | Every turn — it assembles the prompt and parses the reply. The one module that fires on literally every beat. | 18 | `gm_registry.js` `app.js` | **2** | `.communityId` `.descriptionSeed` `.npcsPresent` `.encounterFlavor` | `scene` |
| `engine/names.js` | One name, one home — a display name lives on the record its id points at, and every reference resolves it rather than spelling it. | Every authored name a player reads: a rename changes the record and the prose follows, and SNG-111's progressive naming renders the same person differently to someone who has learned their name. | Prompt assembly and any render of authored prose — a {{kind:id}} token resolves to the current display name. Always, but a no-op on strings without a token. | 6 | `gm_registry.js` | **2** | — | — |
| `engine/narration_voice.js` | The narration's voice for table play — the place's earned register (concrete↔poetic) and the read-aloud constraint, plus a picker that prefers the good installed voices over the default worst one. | The NARRATIVE REGISTER and READ ALOUD blocks, the Settings read-aloud toggle, and the ▶ speak control's voice. | Always (the register colours every beat); the read-aloud directive when spoken play is on. | 7 | `gm_registry.js` `app.js` | **2** | — | — |
| `engine/party.js` | Shared-world play — scene publication, the open-scene index, and merge-safe writes so two players in one scene cannot clobber each other. | The party beat feed and the presence indicator when someone else is in your scene. | Publishing a scene beat in a shared session, and the presence indicator when another player is in your scene. | 16 | `gm_registry.js` `app.js` | **2** | `world/scenes/_open_index.json` | — |
| `engine/places.js` | Location permanence — durable changes, things left behind and discoveries that OUTLIVE the scene, fed back on every return visit. | The location panel's "what happened here" lines on returning somewhere you have been. | A GM placeUpdates op writes durable change; returning to a place reads it back. SNG-176 added recall keyed to the QUESTION, not only to where you stand. | 7 | `gm_registry.js` `app.js` | **2** | `.want` | `moveTo` `placeUpdates` |
| `engine/sense.js` | The graduated sense-of-success filter — the engine always knows the true odds; the character perceives only what their attunement earns, and an opponent's roll is fogged until sense is high enough. Pure. | The precision of the success-chance readout (a vague versus exact odds read) and the fog over an opponent's roll in a skill battle. | Any success preview and any skill-battle round — attunement gates how much of the true math is shown. | 4 | `gambit.js` `app.js` | **2** | — | — |
| `engine/state.js` | Content-pack loading and save/load — localStorage is authoritative, GitHub sync is optional on top. | Boot ("Loading the Valley"), the save/load controls, and the recovery-slot list when a save fails to write. | Boot loads content; every save/load and the recovery path fire it. Always-on infrastructure. | 28 | `gm_registry.js` `app.js` | **2** | `content/packs/core/manifest.json` `content/packs/valley/manifest.json` `the_twelve_reaches.json` `world/regions/valley.json` | `moveTo` |
| `engine/toolkit.js` | The 'what you could reach for' summary — a compact, recency-weighted hold-up of the player's OWN crafts, combines, aspiration, carried items and function-gap, so the GM can offer ONE, lightly. | The TOOLKIT block — options the player may have forgotten, surfaced as a light offer (rule 16B), never a menu. | Assembled each turn; SNG-173 made its quiet-craft pool recency-based so long-unused crafts fade rather than crowd. | 1 | `gm_registry.js` `app.js` | **2** | `.knowledge` | — |
| `engine/waygate.js` | The waygate network and its hub at the Crossing, including routing a GM's moveTo to a real gate rather than letting it mint a destination. | The waygate travel control and the destination list when standing at a gate. | Standing at a gate (the travel control), and a GM moveTo that names a gate is routed through it rather than minting a destination. | 9 | `gm_registry.js` `app.js` | **2** | `.waygate` `.waygateTier` `.waygateHub` | `moveTo` |
| `engine/worldtick.js` | The world moves while you are away — on re-entry after in-game days pass, active events advance on their own clocks, delegated work progresses and arcs ferment, so a crisis ignored worsens and time away is time the world used. | The away-digest news on return — what advanced, what a delegated crew did, what stirred in the world. | Re-entering play after in-game days have passed (runWorldTick plus the SNG-191 generation turn on the world count). | 10 | `gm_registry.js` `app.js` | **2** | `world/regions/valley.json` `.communityId` `.poleIntensity` `.wants` | — |
| `engine/affinities.js` | How a place shapes a roll, both ways and both legible — a TYPE affinity (a tag's flat bump to matching skills) and a VECTOR alignment (an ability whose axes agree with the place's strong spectrum). | The affinity receipt line — why THIS place helped or resisted the action. | An action at a place — the flat and vector modifiers are computed and surfaced on resolve. | 5 | `app.js` | **1** | — | — |
| `engine/backfill.js` | One-time retroactive credit for characters made before engine-XP, bonds and the practice ledger existed — a principled, CAPPED reconstruction from each character's own durable record, never an exact replay. | The one-time credit a pre-feature character receives on load (XP, bonds, practice), shown as its receipts. | Loading a character created before a feature existed, once — the same versioned, idempotent discipline reconcile.js generalizes. | 5 | `app.js` | **1** | — | — |
| `engine/chronicle.js` | The Chronicle reads the accreted self back to the player — one assembled page that WITNESSES who the character has become, from deeds, bonds, standing and the grown arc. | The Chronicle screen and the CHRONICLE block — the character's story so far, plus a one-paragraph 'story so far' synthesis. | A scene closing appends a line; opening the Chronicle assembles the page (app.js owns the async synthesis call). | 10 | `app.js` | **1** | — | — |
| `engine/devcapture.js` | See the machine: a dev-only ring buffer holding the last two-dozen model exchanges — assembled prompt, raw response, parsed result, ops fired — so the by-hand SNG-179 diagnosis (read the prompt, read the raw output, see what the engine did) is a standing instrument, not a devtools dig. | None for a player — it is dev-mode-only (the 🔬 Machine screen behind isDevMode()). Its value is that a defect that used to cost a live play session to find is now visible in a panel. | callClaude invokes the observer app.js registers ONLY under isDevMode() at boot; every model call (GM turn + intent-parse + narrate + generate) routes through that one seam. In a player view the observer is null and nothing is recorded (§3.4). | 5 | `app.js` | **1** | — | — |
| `engine/entityDetail.js` | ONE formatter for the hover/tap detail of the three things that recur everywhere — a skill, an NPC name, an item — so the detail reads identically at every render site. Pure. | The detail card that appears wherever a skill, name or item is shown — the same shape everywhere. | Opening any entity's detail — the app gathers the live values and this formats them. | 4 | `app.js` | **1** | — | — |
| `engine/gambit.js` | Declared multi-step plans — the player lays out a sequence of intended actions with fallbacks, sees what their experience reveals of each step's odds, and executes; steps resolve in order through the same deterministic resolver. | The gambit builder (goal, steps, fallbacks) and the step-by-step run receipts. | The player asking to plan — a gambitOps op opens the builder pre-filled; a failed step blocks the run per its fallback. | 6 | `app.js` | **1** | — | — |
| `engine/gm_registry.js` | The declared table of everything the GM can be told — one row per context block, iterated at every call site so a capability cannot be built and left unreachable. | NONE. It is the §23 wiring contract itself; the player meets it only as a GM that knows things. | Every GM call — assembleGMContext iterates it at all four call sites. NONE player-facing; the player meets it only as a GM that knows things. SNG-182 also resolves name tokens here. | 3 | `app.js` | **1** | `CONTENT.region` `CONTENT.events` `CONTENT.lore` `CONTENT.rules` | — |
| `engine/intensity.js` | The three intensities a craft resolves at — Conserve / Standard / Surge — scaling energy and effect, with backlash risk carried by Surge alone; AUTO picks the minimum the task needs and never auto-surges. | The intensity choice on an ability action and the backlash receipt when a surge costs. | An ability action with an intensity setting — surge is always the player's deliberate choice. | 10 | `app.js` | **1** | — | — |
| `engine/intent.js` | Intent confirmation for costly acts — nothing irreversible or world-scale commits until the player confirms, the same shape character creation already uses. | The confirmation card before a harmful or departing act, naming the cost in plain language. | A costly or irreversible act reaching resolution — the confirmation card is what makes it fire, and it commits nothing until the player says yes. | 8 | `app.js` | **1** | `.connections` | `offerIntent` |
| `engine/pacing.js` | Scene rhythm — the world PUSHES when a beat goes quiet (pressure, escalating and place-respecting) and OFFERS when there is room (SNG-194), so the player never has to ask the world to be interesting. Pure. | The woven THE WORLD ACTS pressure beat and the THERE IS ROOM offer cue — both in-fiction, never a system announcement. | Quiet turns crossing a threshold (pressure); roomForAnOffer computing a positive opening off cooldown (offer). | 6 | `app.js` | **1** | — | — |
| `engine/random_encounters.js` | The living valley — on travel, rest, arrival or world-tick the engine MAY roll one flavored encounter, weighted by the place's dangerLevel and tags, across a tonal spectrum that is never grim by default. | The woven SOMETHING HAPPENS beat when a roll lands. | A travel/rest/arrival/tick roll (SNG-075) — weighted by the location, and never announced as a system event. | 14 | `app.js` | **1** | `.communityId` `.dangerLevel` | — |
| `engine/recurrence.js` | Prestige-driven recurring challengers — a bound arc draws duel challengers as the character's renown rises, the swordsman's paradox as an engine: prestige feeds challengers feeds prestige. | A returning challenger arriving in the fiction, scaled to your renown band. | A renown (deeds-driven reputation) band crossing plus a challenger cooldown — the duel runs as an ordinary SNG-098 skill battle. | 11 | `app.js` | **1** | `.arcId` `.challengers` | — |
| `engine/vectors.js` | Makes a location's spectrum character legible to the PLAYER — a place's strong axes are felt after a visit, subtler axes need attunement or a perceiving ability, and precursor address_sense reveals all. | The place's felt axes shown to the player (the GM already has the full vector); SNG-011 progressive legibility. | Visiting a place, using a perceiving ability there, or precursor address_sense — each reveals more of the vector. | 4 | `app.js` | **1** | — | — |

## GM verbs handled inside `app.js` itself

19 of the reply contract's ops never reach an engine module — `applyTurn` handles them inline. Some of those are correct (a narration flag has nowhere else to live); some are engine logic sitting in the view layer, which is where it gets hard to test.

`arcOps` · `characterDeltas` · `discovery` · `gambitApt` · `gambitOps` · `generateRequest` · `ledgerEvents` · `markTeacher` · `momentArt` · `offerAcquisition` · `sceneEnded` · `standingOps` · `stateOps` · `timeAdvanceHours` · `timeOps` · `unlockLivingCurrent` · `unlockPrecursor` · `unlockSubstrate` · `unlockWildCurrent`

## Blast radius — read this before changing a module

`reach` is the TRANSITIVE count: every module that can be affected by a change here, not just direct importers. A spec touching one of these should state the number before work starts.

- `engine/namematch.js` — **29** modules downstream, serves `offer`
- `engine/quests.js` — **19** modules downstream, serves `questUpdates` `stageOps`
- `engine/playerprofile.js` — **15** modules downstream
- `engine/art.js` — **13** modules downstream, serves `imagePrompt`
- `engine/traditions.js` — **13** modules downstream
- `engine/npcs.js` — **12** modules downstream, serves `npcUpdates` `relationshipDeltas`
- `engine/reputation.js` — **11** modules downstream, serves `deeds`
- `engine/skilltree.js` — **9** modules downstream

## The six lenses (SNG-183) — the defect shapes this batch produced, and where each is caught

Not one of this batch's twelve built-never-reached, three never-fired op-classes, or the lore-blind loader was a *coding* error. Each was a missing **connection** — a value duplicated where nobody updates it, or a chain with a broken link. These are the shapes, stated so they are checked rather than remembered.

| lens | the shape | caught by |
|---|---|---|
| **L1** built-never-reached | an export with no non-test caller | `testOnlyExports` + `importedNeverCalled` ratchets (wiring_audit) |
| **L2** permission isn't initiative | a reachable capability with no *trigger* | this map's **what makes it fire** column — `NONE` on a module with a surface |
| **L3** a guard whose result is not read | `npm test \| grep … && commit` chains on grep's status | discipline: every ship verifies by explicit exit code, no test result across a pipe |
| **L4** orphaned content | a content file no consumer references | loreRef resolution gate (content_ci); `content it reads` column here |
| **L5** never-fired ops | an op with all its wiring that still does nothing | dispatch check (wiring_audit) + the runtime `_opLedger` on the feedback report |
| **L6** a universal gate for a local fact | a global floor encoding a local condition | stranded-encounter check (content_ci): `minDanger` past every tag-home's `dangerLevel` |

L3 is the one that cannot be caught by more verification — it *is* the verification layer — so it lives as a rule, not a gate. The rest run in `npm test`.

