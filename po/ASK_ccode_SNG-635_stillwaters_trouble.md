# Aevi → CCode · SNG-635 — "Raven's Home" keeps coming back, and the source was content

**2026-09-23 · Erik:** *"Raven's Home persists as a name... even though it is actually Stillwater's Trouble. Please fix
or have CCode fix."*

⛔ **The reason your two renames (reconcile §104, `renameHolding`) did not hold is on my side of the line.** The GM reads
the place and its keeper every scene, and both still said it:

- `the_old_warden_post.name` was **"Raven's Home (the Old Warden Post)"** — the map label and every place-line.
- `cassiel_ord` said **"Raven's Home" three times** — his `role`, a `knowledge` line, a `questSeed`. He is the keeper; he
  is in the scene whenever the post is. **The GM was re-coining the name from his record.**

**What I changed (content, this commit):** the place is named **Stillwater's Trouble**; `the Old Warden Post` added as an
alias; **"Raven's Home" kept as an alias**, because smoke 221 asserts it and it is the bridge that lets old save text
resolve here. Cassiel's three lines renamed with `_roleWas` / `_knowledgeWas` / `_questSeedsWas`. The march-road
provenance and the `region_maps` note renamed in place.

## ⬜ What is yours

1. **The saves.** `characters/player-s9z9u1/char-mrhs8286.json` still names it 51 times — holdings, history, the charge
   text, scene summaries. Which of those the engine *shows* is your call; a `CHARACTER_STEPS` rename for the holding's
   live `name` and any display field is what Erik asked for. History and summaries I would leave: they record what was
   said.
2. **`terrain.json`** carries `"n": "Raven's Home (the Old Warden Post)"` — generated by `scripts/world/`, so I did not
   hand-edit it. A regeneration picks up the new name.
3. **`app.js:8385`** writes the kicker from the place name — it will now say *Stillwater's Trouble is yours*. Worth a look
   that nothing there keys on the old string.
4. ⚠️ **A test fixture question, not a defect:** `how_it_works` §104 and smoke 221 use "Raven's Home" in fixtures by design
   (they test the rename). Nothing to change unless you want the fixtures to read the live record.

— Aevi
