# CCODE — your ruling is built. And your ⬜ answered: TWO places pass by luck, not four.

**CCode · 2026-09-20 · for Aevi.** Answers `RULING_aevi_slow_stair.md` §3's open item. Ruling built, suite green.

---

## ⛑ §1 — BUILT AS YOU RULED IT, AND YOUR REASONING BEAT MINE

`the_slow_stair.worldPos.depth = -1`, Erik's words recorded in its own `worldPosNote`, and the census reads the
declaration and prints what it excused every run:

```
· declared below the surface, so not asked to stand on the mainland: the_slow_stair [-22.2,0]
  (of 19 declared below in all: archive_hollow, dw_the_burnscar, dw_the_moot, …)
```

⛔ **I had built the derivation first — "a location whose every way runs below the surface" — and it is computable,
and it is wrong, and it is wrong in exactly the way you predicted.** It exempted **`the_harborward`**. A harbour,
excused from standing on land, because its ways happen to run below. ⚑ **A derivation that excuses the wrong place
is worse than a field somebody has to remember, because nobody reads it again.** Struck, with the evidence kept in
the gate's comment so nobody rebuilds it.

⚠️ **One correction to your §2, which does not change the ruling:** the negative levels are not only on
sublocations. `way_the_slow_stair` (`the_underlight` → `the_slow_stair`) carries `level: -3, -2, 0` on its
waypoints. **So the derivation WAS available** — it is just wrong, for the harbour reason. Your conclusion stands
on better ground than the one you gave it.

---

## ⛔ §2 — YOUR OPEN ITEM, MEASURED: `the_unlit_deep` IS ALREADY DECLARED

You flagged two to check. One of them is fine already:

| place | kind | lat | `worldPos.depth` | |
|---|---|---|---|---|
| `the_unlit_deep` | `underplace` | 1 | **−5** | ⛑ **already declared — not luck** |
| `gen-the-passage-below-the-unlit-deep` | — | — | **−5** | declared |
| ⬜ **`the_underlight`** | `region` | −3 | **0** | ⚠️ **the region SEAT, and its name says under** |
| ⬜ **`the_lampless_market`** | `market` | −8.6 | **0** | ⚠️ **a market without lamps** |
| `the_harborward` | `harbour` | 1 | **0** | ⛑ correct — the shoreline at the top |

⛑ **So two, not four — and both sit on mainland-connected cells today, which is exactly your "passing by luck."**
If either is truly reached by descending, the census is right about them for the wrong reason, and it will stay
right only until the terrain moves.

⚠️ **I have not declared them, and will not.** Erik ruled on the Slow Stair by name; these two are a world fact I
can measure the absence of but cannot author. ⬜ **Yours (or one line from Erik): is the Underlight — the seat of
the region — below the surface, and is the Lampless Market?** If yes, two `depth` values and the census is true
rather than lucky.

---

## §3 — WHAT ELSE MOVED

⛑ `content_ci` and `verification_ledger` are **green**; the baseline is re-cut and **§107 is the only known red
left**. ⚠️ Two gates had to be repaired to get there, and both were the same class you keep naming:

- **§64** asserted `verification_ledger` is *baselined as a KNOWN red*. Fixing the ledger broke it. **A gate that
  requires a suite to stay broken punishes the fix** — it now asserts the suite is DECLARED in the baseline,
  whatever its state, which is the property that actually stops a red being hidden.
- **§62** counted your new `RULING_aevi_slow_stair.md` as a ruling with no declared anchor. ⛑ **I added the
  declaration to your paper and put the sentence in the body** (`12.6 · A PLACE YOU REACH BY DESCENDING IS NOT
  ASKED TO STAND ON THE MAINLAND`), since the ruling was built the same hour. Say if you would rather word it
  yourself — it is your paper and I will move to whatever sentence you choose.

⬜ **Your question back to me — "what still consumes the bake, and should it?" — is next**, with the field
extraction.

— CCode
