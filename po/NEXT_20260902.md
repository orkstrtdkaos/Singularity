# WHAT'S NEXT — organized after CCode's backlog stretch

**Aevi (PO) · 2026-09-02 · HEAD `00cde31d` v1.9.313 · 24 green / 3 red (baseline)**

---

## §0 — THE THROUGH-LINE

⚠️ **Silas is L30 — the exact boundary of R14's band shift** (1–10 personal · 10–30 party building ·
**30–60 Band, outpost, army, strongholds**). ⛔ **Nearly everything now open lives in that band**, and one
item in it is an active bug against Erik's live save.

➡️ **That is the ordering principle below, not spec age.**

---

## §1 — ⛔ ACT FIRST: a live bug is deleting Erik's warden post

**`REVIEW_SNG-358_round2_ccode.md` §0.** `advanceAssignment` is monotonic and terminal — `progress` counts
up, `done` closes the record. Silas's Raven's Home reconstruction is an assignment.

⛔ **When Cassiel Ord finishes it, the post leaves the game's state entirely.** The better the steward
performs, the sooner the station stops existing anywhere the engine can see.

⚠️ **This is not a late-game feature request. It is live state on a played character, at the level where
it fires.** Everything else on this list can wait behind it.

➡️ **CCode:** the holding must outlive the assignment that built it. That is the minimum — the full
Post/Enterprise/Household model can follow.

---

## §2 — ⬜ WAITING ON ERIK — five, each unblocks something

| # | question | unblocks |
|---|---|---|
| **1** | **Braid tier weighting** — how much of a rung does each braid component past the second contribute? | ✅ **SNG-370 entirely** — steps 1–3 are mechanical once the number exists |
| **2** | **`rapport` 14/18** — is the milestone a CAP, or a household that endures unattended? | the six SNG-358 ladder milestones. ⚠️ **If "endures unattended", the second may need NO build** |
| **3** | **Should `creationPickable` exclude `precursor`?** | either reopens OI-19 for **Span**, or corrects SNG-011's wording. ⚠️ One of those is content work and the other is a sentence |
| **4** | **`namedCurrent` (7 crafts)** — does it OVERRIDE `powerSystem`, annotate it, or neither? | ⛔ It makes an access claim nobody resolves. `effectiveLevelReq` gates `living_current`/`wild_current` on real character flags |
| **5** | **`powerMix` (10 crafts)** — `{"wild_nanite": 0.5, "metaphysical": 0.5}`. What does HALF a power system do? | nothing in the engine takes a fractional power system |

⚠️ **4 and 5 are questions of MEANING, not placement.** CCode is right that neither should be guessed.

---

## §3 — ✅ AEVI'S QUEUE, ordered

| # | item | why here |
|---|---|---|
| **1** | **`presence` 14/18/20 milestones** | ⚠️ CCode's own pick, and it is right — SNG-358's dependency is met, `presence` is free to author, and **the milestones sit in the band Silas is entering.** Half need nothing but text |
| **2** | **`sectFlavour` placement** (12 crafts) | ⬜ *"Say where and it is small."* This is a narration call and it is mine. Twelve crafts carry a voice for the sect that teaches them and the narrator never sees it |
| **3** | **OI-20 — ~88 per-rank `backlashRung` trees** | R18's merge is built and its rank term stays inert until these land. The deepening is already wired and waiting |
| **4** | **`npcs/pell.json` and `npcs/veth_ondra.json`** | ⛔ Both are Silas-adjacent, both exist only as interiority entries, and **Pell is the reference implementation for the whole NPC-sheet spec** |
| **5** | **`authoring_gate.py` — two additions** | ⛔ **My gate passed every defect CCode caught today.** It validates the function vocabulary and never looks at `gainAxes`; its `SELFTAX` regex missed nine rank-3 cost defects. ⬜ Ask CCode first whether `certify_counts` now owning `gainAxes` (CCODE-343) makes half of this redundant |
| **6** | **`physicality` / `intimacyNotes`** — ~35 remaining adults | register approved, bounded, ⛔ minors excluded per standing policy |
| **7** | **OI-25 — generative-to-corpus pipeline spec** | ⚠️ Read SNG-369 first — the 63 authored braids **now have a reader**, which may already be half of this |

---

## §4 — ⚠️ THE METHODOLOGY FINDING, and I think it is the most valuable thing in the stretch

CCode: *"the dark-field list is a better backlog than the spec list."*

⛔ **Three specs he checked by guessing module names turned out to be ALREADY BUILT** — SNG-358, SNG-359,
and SNG-369's sibling. A hand-kept list of unbuilt work goes stale the moment someone builds something.
✅ **`field_atlas` is DERIVED, so it cannot.**

⚠️ **This is the same failure I made five times this session in the other direction** — asserting absence
from a partial scan. ➡️ **The general rule both of us keep relearning: prefer a derived instrument to a
maintained list, in both directions.** A guessed name that misses is not evidence of absence, and a stale
list is not evidence of presence.

⬜ **Proposed for `OPERATIONAL_FLOWS_sng.md`:** before opening any spec as "unbuilt", check the derived
instrument — `field_atlas` for authored fields, `certify_counts` for content totals, the ratchets for
behaviour. **Only then read the spec list.**

---

## §5 — ✅ CLOSED THIS ROUND — do not re-open

| | |
|---|---|
| R2 allocation step on all three creation paths | built, gated at the door |
| four creation strings promising the retired antipode wall | removed |
| SNG-369 — 63 authored braids get a reader | built (store still wins) |
| §44 — `traditionV2` monitored rather than wired | ⚠️ **deliberately NOT wired** — it is a stored copy of a derived value; giving it a reader would make the copy authoritative |
| CCODE-343 — `certify_counts` owns `gainAxes` totals | built |
| CCODE-342 — pre-push hook shipped | ⚠️ only one of us had it |
| Committed merge-conflict markers in two doc files | resolved; all 1562 tracked files scanned |
| Doc counts 414/423 → **419** across `HOW_IT_WORKS`, `PLAYERS_GUIDE`, `FIELD_REFERENCE` | corrected |

---

## §6 — ⛔ SNG-370 §2a — why it was reported and not built

*"The ledger is one loop, but the arity gates are shut — building half would dig a fourth door."*

✅ **That is the right call and it should be said plainly:** a partial build here creates a fourth access
path into a system that already has three. ➡️ **It stays blocked on Erik's §2 question 1 and should not be
started before it.**
