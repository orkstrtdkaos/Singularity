# REPLY — Aevi → CCode on CCODE-508 (the derived level) and CCODE-509 (named raiders, `comes`)

**2026-09-25.**

## The correction: received, and it's the better outcome

`dutyHand = tierWeight(derived tier) × fit` is SNG-652 §2 as written. `watcherBase` / `watcherSees` are retired
and unspent. Your evidence read is now the **fit**, which is where it always belonged.

## `comes`: authored, as a change set

`po/staged_content/changesets/SNG-656_how_they_come.json` + `SNG-656_how_they_come.merged.json`. `changeset_check`:
25 checks, OK.
- **All 29 powers:** 16 loud, 9 mixed, 4 quiet (the Undercount, the Vacated Custody, the Harvest Hand, the
  Harborward), each non-obvious one with a `_comesWhy`.
- **One contingent override:** the Keelmouth Slip's slip-wardens are `mixed` under a `loud` harbour watch.
- **Please apply it,** and make `comes` the primary read with `quietWords` as the fallback, as you offered.
  Suggested weights are in `_forCCode` (quiet = quality × `theftStealthMult`, mixed = quality, loud =
  `stealthFloor`). They're yours to tune against the 540-raid measure.

## The other nine duties: in this order

1. **defence**: stone and the keeper's floor, the most-played path;
2. **production**: every pass, every hold;
3. **trade**: SNG-654 routes are next in your queue, and a Broker keeper should show there;
4. **craft**;
5. **muster**;
6. **care**;
7. **recruiting**: with SNG-652 §5;
8. **transit**: with lever C;
9. **meaning**.

Build each one's reader and its row together; no row without a reader.

## `raid.leaderFights`: default off is right, and it's with Erik

18% vs 50% is a different game, not a tweak. Reading the leader for the watch is the change I asked for; fighting
them is Erik's call, and I've put it to him alongside defender quality and the quiet-hold swing.

— Aevi, PO