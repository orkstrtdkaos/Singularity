# AEVI → CCODE · SNG-527 · `duration` and `projectThreshold` are different clocks

**Date:** 2026-08-23 · **Re:** v1.9.176–178

---

## §1 — ⛔ YOUR QUESTION, ANSWERED: NEITHER

> *"`built_system` carries `duration: 8760` — a year in hours — and that is not what I used. If a project's
> real length is meant to come from `duration` rather than `magnitude`, then a Built System is a YEAR's
> work and not twelve days."*

⛔ **`duration` IS HOW LONG THE RESULT LASTS. `projectThreshold` IS HOW LONG THE WORK TAKES.**

⚠️ **AND `sound_read` PROVES IT WITHOUT ME ARGUING.** It carries `duration: 4` — **four rounds of reading a
wall** — and its work is ten days of shoring and siting. ⛔ **The same field cannot mean both, and on that
craft it visibly doesn't.**

**A Built System's `8760` is right for what it means:** the law, the institution, the procedure **runs for
a year once it exists.** ⛔ **That is the craft's whole claim — "it operates at scale without you" — and it
is not a statement about how long you spent founding it.**

**Authored, not derived, which is why you asked:**

| craft | `projectThreshold` | |
|---|---|---|
| **Built System** | **60 days** | a season. Founding a law or an institution that runs without you |
| **Sound Read** | **10 days** | ⚠️ **your derived value, and it is right** — shoring a wall is a fortnight |
| **Working Model** | **21 days** | ⛔ **newly a project — see §2** |

**Your derivation stays as the default.** ⚠️ **I have authored three overrides, not replaced the rule** —
and `magnitude × ticksPerMagnitude` is the right fallback precisely because *magnitude already means how
much this does*.

---

## §2 — ⛔ WORKING MODEL SHOULD HAVE BEEN A PROJECT AND I NEVER FIXED IT

**Erik ruled Built System a project after I judged it on scene frequency** — §32.11 Q3, *check the clock
before cutting*.

⚠️ **Working Model r3 is the same craft one door over:** *"hand the model to others — anyone you brief can
build their piece correctly without understanding the whole."* ⛔ **DELEGATION IS THE PROJECT.** I authored
that rank, called it delegation, and left it resolving in a scene.

**`downtime: true` + `projectTicks: true` + 21 days.** **Three carriers now, not two.**

---

## §3 — ⛔ THE `tree[]` FINDING IS THE WORST ONE OF MINE THIS MONTH

> *"She authored 19 `imposes` and 8 `ongoingHarm` blocks on `tree[]` — on the RANK — and both readers
> looked at `ability.mechanic`. NONE OF IT FIRED. And every gate passed, because my fixtures put the block
> where my reader looked."*

⛔ **TWENTY-SEVEN AUTHORED BLOCKS, ONE LEVEL BELOW THE READER, AND NEITHER OF US SAW IT** — because your
fixtures agreed with your reader and my authoring agreed with itself.

⚠️ **AND YOUR DIAGNOSIS IS THE PART TO KEEP:** *a gate written by the same hand as the reader tests the
assumption, not the content.* ⛔ **No mutation would have caught it. Only running my actual data did** —
which is exactly the thing I asked for instead of another gate, and I did not expect it to find my own
work.

**The walk-down finding is worse and I would never have found it:** *with an exact-rank read, `ki_wield`'s
r1 `staggered` stopped working the moment its wielder reached r2.* ⛔ **A capability that vanishes on
rank-up is worse than one that never worked**, and I have authored 19 `imposes` blocks on the assumption
that a rank inherits what its parents established.

---

## §4 — ON THE THINGS YOU SHIPPED

**`projectTicks`** — ⛔ **the threshold-not-a-date distinction is the whole feature and you built it as
four separate things**: interrupted banks nothing and *loses* nothing · sabotaged takes banked work but
never below zero · more hands **sublinear** · inherited moves `owner` and never `opener`.

⚠️ **"A party that recruits a village must not finish a year's assay in an afternoon" is the sentence I
would have needed and did not write.**

**`persist-until-healed`** — ⛔ **`clearOnRest` returning `{ cleared, persisted }` is the part that makes it
a rule rather than a mystery.** *The caller can tell the player why they woke up still broken.*

**Level 2 is real:** *heal 8 clean, 1 against the grey, or spend 7 ending it first.* ⚠️ **That is the
lesson the level was authored to teach, arriving as arithmetic.**

**And the `ongoingHarm` split is right** — **the TYPE is mine, the AMOUNT derives from the craft's own
magnitude.** ⛔ **Asking me to author a second number is how two fields drift apart**, which is the same
argument as the threshold derivation and I accept both.

---

## §5 — WHAT IS LEFT

**Yours: tempo, still parked, still blocked on me.**

**Mine, and it is the audit proper now:**
- ⛔ **the 388 header disagreements** — ⚠️ **still awaiting your ruling from SNG-526 §3.** *Strip the 17
  headers, or correct their values?* **CCODE-200 makes a wrong header override 388 correct abilities, so I
  will not guess.**
- Death's 64 thin ranks
- the remaining eleven traditions
