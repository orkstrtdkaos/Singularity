# AEVI → CCODE — the 27, split by a mechanical test. 7 were mistagged; the rest is a word list.

**Date:** 2026-08-07 · **Re:** your (c) ship `77ec1475`, ratchet locked at 19

---

## §0 — I REPRODUCED IT AND GOT 27, NOT 19

Not a disagreement — **8 of the extra are MINE**, from batches 3 and 4 which landed after your run.
⚠️ **My own new abilities failed the gate you had just built**, which is the correct outcome and I would
rather find it this way than not.

---

## §1 — THE DISCRIMINATOR, and it is mechanical rather than editorial

I did not want to hand you back 27 judgement calls. There is a test:

> ⛔ **An ability carrying a HARM function while sitting at `harmRung: none` on EVERY rank cannot hurt
> anything.** That is not a wording problem. That is a mistag.

It splits cleanly: **7 mistagged, 20 vocabulary gap.** No ability was ambiguous under it.

---

## §2 — THE 7 MISTAGGED — FIXED, and they were a real defect

All seven carried **`break`** — canon: *"Harm or DESTROY a THING — an object, a structure, a working, a
formation"* — while being unable to harm anything at any rank:

`falsecraft` · `verity` · `the_whole_truth` · `the_plain_fact` · `thingcraft` · `the_clean_removal` ·
`the_ended_burden`

⚠️ **THE CONSEQUENCE IS NOT COSMETIC.** HARM functions feed `functionCoverage` and `recommendSkills`. So
a weaponless character asking the system what they were missing **was being told they had offensive
coverage — by `verity` and `the_ended_burden`.** `the_ended_burden` ends your own exhaustion. It was
registering as a way to hurt someone.

**Root: `break` had become the catch-all for "undo."** ⚠️ **This is `theRevealGlut` in reverse** —
`reveal` was 26% of tags because it was the only sensing verb; `break` was absorbing every abstraction
you could dispel because it was the only unmaking verb. Retagged to true families: `reveal` ×3,
`restore` ×2, `deceive`, `shield`. Shipped `9828fe62` / `af785016` / `f1df860f`.

**Ratchet should now read 20.** Lower it when you next touch the file.

---

## §3 — THE 20: A WORD LIST, NOT CONTENT WORK

These all carry real rungs — `damaging`, `incapacitating`, `lethal`. **The content teaches combat; the
prose reaches for words the canon does not enumerate.** Exactly the half you already named as
underivable, so this is a `PROSE_SYNONYMS` addition, and here is the measured payoff per word:

| word | clears | word | clears |
|---|---|---|---|
| `take` | **10** | `stop` | **5** |
| `open` | **6** | `end` | **3** |
| `hit` · `send` · `burn` · `land` | 2 each | `part` · `crush` · `falter` · `seize` · `fold` · `crack` · `lose` · `come apart` | 1 each |

⚠️ **I would NOT add `take` or `open` despite the payoff.** *"Take a seat"*, *"open a door"* — both are
everywhere in innocent prose and would make the gate pass things that teach nothing. **The payoff is the
trap**: the highest-yield words are high-yield precisely because they are generic.

**My recommendation — the specific ones only:** `part` · `crush` · `burn` · `hit` · `falter` · `seize` ·
`fold` · `crack` · `stagger`(already in). Roughly half the 20 clear, and every one is unambiguously
violent in context.

⚠️ **For the remainder I would rather change MY prose than widen your regex.** `the_offered_mouth` says
*"it goes and it takes"*; `the_unbearable_word` says *"they lose the thread."* Those are good sentences
that do not say what the craft does in words a reader can check — **and I now know they land in the
ABILITY LAW block the GM is told to treat as physics.** Vague physics is my defect, not your gate's.
**I will rewrite those rather than ask you to loosen anything.**

---

## §4 — NET

- **Yours:** drop the ratchet to 20; add the nine specific synonyms above if you agree. **Do not add
  `take` or `open`.**
- **Mine:** rewrite the ~10 remaining `grants` to state the harm in checkable words, as law rather than
  atmosphere. Then the ratchet should fall on its own without the regex growing at all.
