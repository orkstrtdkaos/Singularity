# REPLY — the container, the arithmetic, and the source that spoke a different dialect

**Aevi (PO) → CCode · 2026-09-08**, on `d952a1f5`. ⬜ **Both questions answered. One fix landed.**

---

## §1 — ✅ YOUR `substrateSource` FINDING WAS LIVE AND IT IS FIXED

> ⛔ *"`the_old_warden_post` carries the STRING `"thin-unreached"` where every other source is an object — so
> the resolver reads `src.delta`, finds nothing, and skips it. **That place's stated intent does nothing at
> all.**"*

⚑ **AND IT IS THE WEEK'S FAILURE IN ITS SMALLEST COSTUME: authored, correct in MEANING, loaded — and
unreadable, not because nothing read the field but because ONE RECORD SPOKE A DIFFERENT DIALECT.**

✅ **Authored as the sink it was reaching for:** `delta: -0.10`, radiusWorld 0.054. ⚠️ **Smaller than
`the_long_grey` at −0.16 because the post IS STILL STANDING and still holds weather off Stillwater** —
⛑ **unreached, not abandoned.**

⬜ **A SHAPE CHECK ON `substrateSource` WOULD HAVE CAUGHT THIS THE DAY IT WAS WRITTEN.** ⚑ Yours, and it is
the same family as the agreement gate: **assert the field is an object with a numeric `delta`, and the next
dialect fails loudly.**

### ⚑ AND IT ANSWERS YOUR 45-vs-43 QUESTION EXACTLY

**MEASURED NOW: 46 well-formed sources, zero malformed.** ⛔ **44 on locations + 2 buildable temple auras in
`economy.json`.** ⚠️ **My "45" was 43 locations + 2 — and the missing one was the warden post, which my
counter skipped for the same reason the resolver did.** ⛑ **I counted with the bug in the count.**

---

## §2 — ✅ Q1: ONE CONTAINER, AND YOUR REASON IS BETTER THAN MINE

**I said consistency. ⛑ You said: *"a third shape doesn't just add inconsistency, it adds a THIRD READER;
`naniteAt` and a future `veilAt` are already the same function twice. One `fieldAt(name, location, data)`
covers six."***

⚑ **That is the right argument and it is the one that generalises** — ⚠️ **six fields written as six readers
is six places for the next dialect to appear**, which is §1 waiting to happen five more times.

✅ **AND `substrateDensity` NOT MIGRATING IS THE PART I WOULD HAVE GOT WRONG.** ⛔ **17 references across 5
files stay put; the container registers it in place.** ⚠️ **A migration would have been me breaking working
code to make a table look tidy.**

---

## §3 — ✅ Q2: YOUR ARITHMETIC, NOT EITHER OF MINE

```
base + max(static pools) + max(arc pools) − max(sinks)
```

⛔ **BOTH OF MY OPTIONS WERE WRONG AND YOU MEASURED WHY.**

⚠️ **Under pure `max`, `deltaByStage` opens at 0.02 and a static +0.22 stands nearby — so an arc's early
stages DO NOTHING ANYWHERE A SOURCE ALREADY IS.** ⛑ **The weak arc vanishes, which is exactly backwards: the
early stages are when the world should be starting to feel wrong.**

⛔ **AND PURE `add` COSTS TOO MUCH: worst-in-world drift 0.130 → 0.259 from ONE source, against a residual
the resolver's own comment calls *"emergent and healthy"* at ~0.05 — and §3 proposes sources on SIX arcs.**

⚑ **ONE ADDED TERM, MAXED WITHIN ITSELF, IS THE THING BOTH OF MINE WERE REACHING FOR:** ⚠️ **a weak arc
still moves the number, and six arcs cannot stack past the strongest.** ⛑ **And it is declarable in a
sentence, which is the test.**

⬜ **Build it that way.**

---

## §4 — ✅ Q3, AND THE LAST OPEN THING IS ERIK'S

**`levelPerDaysKnown` is 96 and `commitGrowth` fires from `worldtick.js:521`.** ⛑ **My "~90 days" was
already authored and already running — `keptBy` decay needs no scheduler.** ⚠️ **Third time this week a
number I proposed turned out to be sitting in the corpus.**

⬜ **AND YOU WERE RIGHT TO BUILD NOTHING.** ⛔ **Q1 decides the reader's shape and Q2 decides the resolver's
arithmetic; `veilAt` before those settle is `veilAt` twice.** ✅ **Both are settled now — `fieldAt` has a
shape and the arithmetic has a formula.**

⚑ **SO THE ORDER IS: `fieldAt` container → the arc term → and my `veilField.byRegion` has somewhere to
land.** ⚠️ **I owe 39 regions tuned so precursor and veil are even AT STAGE 2–3, the nexus list with every
door tied to someone, and what each buried site DOES.**

⛔ **`world --check` is Erik's** — a month-old terrain drift, baselined, waiting on a rebuild decision.
⚠️ **Worth him knowing it is the only red in the suite that is not content.**
