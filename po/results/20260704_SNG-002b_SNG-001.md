# Results — SNG-002b + SNG-001 (phase 1) + creator addendum
Date: 2026-07-04 · Shipped: v1.2.0 · Tests: 198/198 smoke + parse_probe PASS

## SNG-002b — lethal avoidability clamp: SHIPPED
- `lethalOfferClamp` (encounters.js) applied at choice render: lethal offers never trivial, stakes forced into the label, guaranteed decline choice appended when missing. Round-1 flee: UI flee button is unconditional for duels (availability never gated; difficulty applies).
- Rule 18 += ratified sentence. Smoke: lethal-offer-carries-decline, entry-never-trivial, no-duplicate-decline, non-lethal-untouched.

## SNG-001 — shared scenes phase 1: SHIPPED
- `engine/party.js`: pure core (newSharedScene/addMember/removeMember/mergeBeat idempotent by (by,at)/round-robin nextTurn/isMyTurn/partyBlockForGM/setEncounterState) + transport (fetchScene, listScenesAt via new sync.ghList, pushSceneWithMerge refetch-merge-retry ×3).
- App: Party sidebar (sync-gated), find/start/join/leave, 20s poll, client turn gate (Ask GM open off-turn), remote beats → chronicle + aside, my beats published post-turn with witnessed encounter receipt serialization. GM PARTY context block (no load-bearing contract change).
- Verification: criteria 1 (198 tests incl. 12 party: merge idempotency, turn rotation, gate, encounter witness block, double-writer conflict replay), 4 (browser: sync off → no party UI, solo intact), 5 (conflict simulated in smoke), 6 (partyBlock carries encounter receipt — smoke). **Criterion 2/3 (two-browser live) needs Erik's second-device leg** — engine + UI paths verified single-browser; flagging per spec option "Erik browser-leg".

## Creator addendum (Erik, direct): map & sub-places — SHIPPED
- `placeUpdates.subPlace` GM op + rule 15B sentence; place memory stores per-location sub-places (slug-deduped, visited/heard-of, cap 12); map details panel shows chips (click = in-location move; cross-location guarded); sidebar travel buttons retired → "Map & Rest" section; GM place-history block lists known places within.

## Spec boundaries
- Two-browser live verification deferred to Erik (documented above).
- parse_probe.mjs semantics fixed (SyntaxError = fail; DOM TypeError = pass) — was inverted-ish.
