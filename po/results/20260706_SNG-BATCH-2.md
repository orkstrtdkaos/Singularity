# Results — SNG-BATCH-2 (Phases 0-3)
Date: 2026-07-06 · Shipped v1.5.0 → v1.6.1 · Suite green at every phase boundary (327 checks) + parse_probe. Browser-verified.

## Phase 0 — SNG-012 Memory & Input Fidelity: SHIPPED (v1.5.0)
- **Part A (input fidelity):** player's RAW typed text now reaches the narration GM verbatim as a `## PLAYER'S EXACT WORDS` block (threaded onFreeform→onChoice→runGM→buildTurnContext); parseIntent stays mechanical-only (drives the roll). GM rule EW added.
- **Part B (facts ledger):** `engine/facts.js` — durable NON-SCROLLING establishedFacts ledger; GM `factUpdates` op (add/resolve), fed IN FULL every turn as `## ESTABLISHED FACTS (authoritative, persistent)`; cap 40, deduped. NPC `statusNote` (npcUpdates) pinned every turn in npcRegistryForGM. Chronicle window -12→-16. GM rule FCT added.
- **Part C (name fix):** op-loss restate now explicitly re-emits pending reveals/facts; **player-driven NPC rename** — "✎ name" affordance on name-unknown People-You-Know entries (nameIsUnknown heuristic), setNpcName sets displayName on stable id, logs it, GM carries it thereafter.

## Phase 1 — SNG-011 world legibility: SHIPPED (v1.5.1)
- Map sub-place satellites + precursor tier + peril drift were already shipped v1.4.0. This phase added **location vectors made player-perceivable** (`engine/vectors.js`): strong axes (|v|≥0.5) felt on visit; mid axes (≥0.3) via attunement≥6 or a perceiving ability (prism_sight/old_roads); address_sense reveals ALL exactly; vectorsKnown per-place; shown in the map details panel. Optional vector-resolution-bias remains UNRATIFIED (not built).

## Phase 2 — skill legibility: SHIPPED (v1.6.0)
- `engine/skilltree.js`: Tier I–V from levelReq, class colors, graph model. Tier badges everywhere (sheet, picker, graph). Ability picker now collapsible by class with tier badge + levelReq + capacity/gate lock indicators. Rank-up highlight (new grants + new limit). **Skill-KG graph** rendered like the world map — class columns, tier-sized nodes, emergence-recipe edges, owned/ripe/aspired/locked states, click-for-detail; reachable from the character screen.

## Phase 3 — attribute gates + capacity: SHIPPED (v1.6.1)
- Both pre-authored tables (attribute_gates.json, skill_capacity.json) loaded into CONTENT. Engine ENFORCEMENT in learnAbility/rankUpAbility: gated ability needs governing sub-attribute ≥ learnMin to learn, ≥ rank3Min to reach rank 3; broad-vs-deep capacity blocks a fresh learn at the breadth cap (rank-ups always allowed; earned techniques — discoveries/branches/bond grants — exempt). Capacity indicator on sheet + picker; locks shown in graph + picker.

## Phase 4 — SNG-010C item evolution: DEFERRED
Optional per spec ("only if room after 0-3"); requires PO-authored Waystaff+Aevi seed content ("PO authors on pickup, not now"), which is not at origin. Correctly deferred to a PO content pass.

## GM contract additions (Erik ratification requested)
Rules EW (exact-words narration), FCT (established-facts permanence + factUpdates + statusNote), and rule 19 emergence/precursor (shipped prior batch) are load-bearing — flagged for Erik's ratification per protocol.

## Erik browser-leg (verify list)
1. Long typed action honored by narration. 2. Rescue an NPC → still rescued many scenes later (fact ledger + statusNote). 3. Map sub-place satellites. 4. Location vectors; subtle axes need a perceiving ability. 5. Skill graph from character screen (class-colored, tier-badged, level-reqs, recipe edges). 6. A Tier-III+ ability shows an attribute lock until the sub-attribute is high enough; at breadth cap, learns block and points deepen.
