# Results — SNG-BATCH-3 (World Liveliness)
Date: 2026-07-06 · Shipped v1.6.2 → v1.6.4 · Suite green at every phase boundary (378 checks) + parse_probe. Browser-load-verified.

## Phase 1 — SNG-014 Random encounters (flavored) + dev test triggers: SHIPPED (v1.6.2)
- **engine/random_encounters.js** — on travel/rest the engine may roll one flavored encounter, weighted by the location's dangerLevel + tags: peaceful flavors (beneficial/benign/beautiful) skew safe places, perilous (dangerous/theft/chase/fight) skew dangerous — with a grace floor so every place keeps a chance of beauty. `rollTrigger` respects the content chances (settled/hearth rests are safe); `pickEncounter` weights + gates by minDanger/region/tags.
- **Routing reuses existing engines, no new resolution math:** narrative/opposed → GM scene + resolve.js (opposed names the check); challenge/duel → a synthesized typed def handed to encounters.js (`synthesizeDuelDef` maps the light opponent block to the full duel schema; `synthesizeChallengeDef` builds staged chases/hazards).
- **HARD guardrail met:** every lethal-capable encounter presents an engine-built **decline/flee choice BEFORE engagement** (not left to the model). Owned peaceful-out abilities (Beastfriend ends a creature chase; Mediator's Tongue defuses a fight) are offered as encounter-ending choices.
- **Dev test triggers:** `?dev=1` (or localStorage `singularity.dev=1`) shows a 🔧 panel with one button per flavor, firing on demand with the danger-gate bypassed — this is how combat is finally testable. Not shown in normal play.
- Precursor-glimpse flavors stay glimpsed-never-explained.

## Phase 2 — SNG-013 Location skill affinities: SHIPPED (v1.6.4)
- **engine/affinities.js** — two layers, both surfaced to the player. (1) TYPE affinity: a location tag grants flat capped bumps to matching skills/attributes, bridged from freeform location tags via a content `tagAliases` map (wilds→wild, riverside→water, archive→ruin, …). (2) VECTOR alignment: an ability whose axes agree with the place's STRONG spectrum axes eases the roll, opposed hardens it. Folded into the resolve path + choice-preview chance + roll receipt + GM resolution block.
- **Perception-gated reveal (SNG-011 tie):** a perceived axis (vectorsKnown) is named in the receipt ("+7 aligned to concrete/abstract"); an unperceived one reveals vaguely ("the place favored this").
- **Aevi amplitude reconciled:** Aevi concurrently authored an amplitude system where charged places multiply the vector cap (precursor ×2.0, ruin/shrine ×1.5, per-location overrides; effectiveCap = round(baselineCap × amplitude), clamped 8..24). The engine (`effectiveVectorCap`) honors it — baseline places cap at ±10, the Archive Hollow (×2.0) at ±20. Numbers stay Aevi-tunable in content.

## Out-of-band fixes shipped this session (separate commits)
- **fix(party):** shared-scene create/join now surfaces the real GitHub error (403/404/401) as an actionable sentence instead of a generic "sync failed." Root cause of Erik's report was a token-permission 403 (PAT read-only) — a config fix on his side, now self-explaining. (v1.6.3)
- **fix(pages): `.nojekyll`** — GitHub Pages' default Jekyll build had been FAILING on every push (site frozen at v1.6.1, looked like cache lag). Disabling Jekyll fixed the deploy. Diagnosed via the public Actions API (three `completed/failure` runs). Correct config for a pure static ES-module app.

## Erik preview-leg (open — his live test)
Erik opted into preview testing; he's testing combat now on the deployed site via `?dev=1`. Test tasks handed over: fire a Fight (decline/flee before engagement, duel runs in rounds with both health bars moving, yield ends cleanly), plus Beautiful + Theft for tonal range; and in a strongly-tilted place attempt an aligned vs opposed skill and confirm the receipt shows the place helping one / hindering the other.

## Ratification flags (unchanged from batch open)
GM rules EW / FCT (SNG-BATCH-2) still awaiting Erik's confirm. No new GM contract rules in this batch beyond the LOCATION AFFINITY narration cue (engine-computed, GM narrates).
