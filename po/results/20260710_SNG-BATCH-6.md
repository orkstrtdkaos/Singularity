# Results — SNG-BATCH-6 (Foundation)
Date: 2026-07-10 · Shipped v1.7.1 · Suite green (475 checks) + parse_probe. Live-verified in preview. Status: **shipped, awaiting Erik preview-legs before complete_pending_review** (per batch instruction).

## Phase 1 — SNG-019 Codex entity-resolution: SHIPPED
- **Alias resolution on write** (`engine/codex.js` → `resolveTopic`): every codexUpdate resolves before minting — explicit `entityId` first, then known-entity name anchoring (met people from the registry + authored NPCs + world locations, passed as `ctx.entities` from the turn pipeline), then conservative label/alias matching (normalized equality, or whole-word containment ≥4 chars — "Teva" ⊂ "Teva the healer"; "Mara" never matches "Maren"). Topics gain `aliases[]` (every GM phrasing recorded) and `entityId` anchors; a late entityId upgrades an existing unanchored node.
- **GM contract nudge:** `codexUpdates` gains `entityId` ("PREFER this — it anchors the fact to their one node"); rule 17 instructs the GM to ALWAYS pass the stable id for anyone in KNOWN PEOPLE / any place id it holds; free-text topic only for genuinely new things.
- **Merge tool** (`mergeCodexTopics`): high-confidence-only collapse (same entityId, or alias/label match with compatible kind) into the primary node (anchored > kind-rank > fact-count); facts concatenate deduped + chronological, links/aliases union, inbound links rewire; idempotent. Runs automatically on old saves as **reconcile step v1** (Phase 2) — the two phases tie here by design.
- **Caps:** anchored primary nodes hold **24 facts** (major NPCs); ordinary topics keep 12; aliases cap 8.
- **Codex UI:** grouped by kind (People / Places / Factions / Events / Mysteries / Lore), primary (anchored ◈) nodes first, latest facts nested chronologically under each node, aliases shown inline + on the topic page, linked nodes surfaced as buttons.
- Smoke: differently-phrased facts land on one node; entityId beats label; place phrasings collapse; merge heals a fragmented save + rewires links + is idempotent; primary cap honored; conservative matcher rejects near-names. **Live-verified:** three phrasings ("Teva" / "Teva the healer" / "the woman from the chamber"+entityId) → 1 node, 3 facts, 2 aliases, anchored `teva`.

## Phase 2 — SNG-022 Reconciliation engine: SHIPPED
- **`engine/reconcile.js`** — a single `reconcile(entity, kind)` pass generalizing backfill.js's discipline (versioned, idempotent, one-time, principled) into a **migration-step registry**. Per-entity `reconcileVersion` gates idempotence; steps self-check preconditions and are order-independent; a broken step warns and never blocks load. backfill.js remains the XP/bonds/practice credit pass — extended, not replaced.
- **Character steps:** v1 `codex-entity-merge` (SNG-019's one-shot repair; player-facing) · v2 `additive-fields` (safe **empty** defaults for establishedFacts / forkChoices / precursorAccess / customAbilities / customEncounters / placeMemory / companions / discoveries — never fabricates). Runs in migrate() on every character load.
- **Content steps** (run inside `loadContent`, in-memory — Pages files are static; authored files get fixed upstream, the runtime stays self-consistent either way): v1 `pole-intensity` — any location with spectrum but no poleIntensity derives it (spectrum axis `neg_pos: v` → `{pos: v}` / `{neg: |v|}`, pole names from spectrums.json; **verified byte-identical to the hand-authored shape** on millbrook) · v2 `crossref-integrity` — connections referencing unloaded locations are FLAGGED (console.warn), never removed.
- **GRANTS offered-not-imposed:** the pass returns `offers[]` which the app surfaces as choices — never auto-applied. Architecture proven with a synthetic talent step in the suite; the real innate-talent step registers here when SNG-017's talent content lands (none exists yet — none owed this batch).
- **Login UX:** player-facing reconciliation surfaces as a **"The world has grown"** aside on entering play (merged with the backfill catch-up aside when both fire); silent for pure-plumbing steps.
- Smoke: owed steps apply on first run + stamp reconcileVersion; second run changes nothing; defaults are empty (no history fabricated); offers surface without applying; a throwing step is contained; missing poleIntensity derives / authored untouched / dangling connection flagged-not-removed; content pass idempotent. **Live-verified:** 6/6 loaded locations carry poleIntensity after load (echo_river_crossing derived from its all-negative spectrum).

## The 2 post-BATCH-5 reds — NAMED + ROOT-CAUSED (fixed, not wave-folded)
Both were suspected to originate from the caching restructure. **Disproven:** both fail at origin commit `4d85cc7` with the caching commit (`d32b45d`) absent from the tree (verified by stash-test during the caching session, re-confirmed by archaeology). Actual root causes are the 2026-07-07 content wave:
1. **`catalog is 28+ abilities with trees`** — the catalog expansion (~28 → **117** abilities) added tradition packs whose entries are 1-rank seed abilities without `notFor` (~60 of them). The old every-ability invariant no longer describes the authored corpus. **Fix:** test now asserts the preserved contract — 28+ fully-treed core abilities (3 ranks + notFor) AND every ability engine-safe (id/name/description/energyCost/powerSystem). *Open content note for Aevi: the ~60 seed abilities eventually want full trees + notFor; engine handles 1-rank trees safely meanwhile (rank expressions fall back to description).*
2. **`discovered technique earns bonus instead of surcharge`** — commit **`6bfb98d`** ("tune: raise novelty rewards", Aevi/Erik) raised `novel.discoveryBonus` 10→20; the fixture's `plainChance + 20` now crosses `d100.ceilingChance` (95), so the unclamped equality fails. **Fix:** test asserts the clamped contract (`min(ceiling, plain + bonus)`, still strictly above the novel-surcharge path).

## Erik preview tests (one line each, live on v1.7.1)
1. **SNG-019:** "Open your Codex (from the play screen) — verify it's now grouped by kind with facts nested under each person/place, and that Teva (or any much-discussed NPC) shows ONE entry with her scattered mentions gathered under it (aliases listed) rather than several near-duplicate entries."
2. **SNG-022:** "Load a character — if your codex had duplicates you should see a 'The world has grown' note on arrival; then play a beat or two and confirm nothing about your character regressed (abilities, facts, items all intact)."

## Guardrails held
Engine-only (no content authored); design law 1 intact (resolution/merge/derivation engine-owned, GM narrates); additive schemas (`aliases`, `entityId`, `reconcileVersion` — all backward-safe); derives-never-fabricates; grants offered-not-imposed; suite + parse_probe green.

## Addendum (2026-07-10, Erik preview feedback): player allocation layer — SHIPPED v1.7.2
Erik's leg 1 verdict: grouping works, but his live save still held many Teva entries under headers too different for the high-confidence auto-merge (by design — it must never eat near-names). Built the spec's optional player-facing layer:
- **`mergeInto(source, target)`** — fold ANY entry into ANY other; player judgment overrides every heuristic. Facts move + re-sort chronologically; the old header becomes an alias. UI: **"⇆ This is the same as another entry…"** on every topic page (suggested targets first).
- **`suggestMerges()`** — medium-confidence candidates (shared name-token via label/alias, mutual/overlapping links, kind-compatible) surface at the top of the codex as **"These look like the same thing — your call"** with Merge / Not-the-same.
- **`markNotSame()`** — a "Not the same" verdict persists on the save and excludes the pair from suggestions AND auto-merge forever.
- **Standing tidy** — the high-confidence pass now runs on every load; each manual merge enriches aliases, which cascades further auto-collapses (verified live).
Live-verified end-to-end in the preview UI (fragmented fixture → suggestion merge → manual allocation → one node, 4 facts chronological, 3 aliases, persisted). Suite 486 checks green.
