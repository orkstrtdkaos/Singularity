# Results — SNG-043 (Gambit refinement — surface less, build better)

Date: 2026-07-11 · Shipped v1.8.6 · 689 smoke + parse_probe green · boots clean. Status: **shipped, awaiting Erik preview-legs before complete_pending_review**. Only Aevi closes.

Refines SNG-031 (BATCH-8). Both parts built.

## Part A — tightened surfacing (the "hint fires every scene" fix)
`isGambitApt` (app.js) now requires a **real plan signal**, not mere richness:
- Fires on a **strict plan-intent tag** (`plan` / `scout` / `prepare` — `investigate`/`analyze` dropped, they're single-action) **OR** an explicit **staged/multi-obstacle objective** (a GM-flagged `staged`/`multiStep` choice, or a scene carrying ≥3 named threads to sequence through).
- The loose `abilityChoices >= 2` and `nonTrivial >= 4` fallbacks — which fired on nearly every substantive scene — are **removed**. Rich ≠ plan-apt.
- Still dismissible-per-scene. Net: the Plan hint is now occasional + special, matching "presented when the scenario presents itself."

## Part B — build better
- **Energy-cost feedback:** once a plan is assessed (steps parsed), the builder shows **each step's energy cost** — the ability's effective cost when the step uses one, else the flat per-step default (`gambitStepEnergy`) — and a **running total vs available energy**, with an **over-budget warning** before commit ("this plan costs more energy than you have; expect to fall short partway"). The feedback that lets a player budget a plan.
- **GM-collaborative building:** a **"✦ GM, look at this"** button in the builder asks the GM (via `gmAsk`, the OOC meta channel) for **ONE concrete suggestion or warning on the DRAFT plan** — sequencing or risk — shown inline as advice, nothing attempted. Interactive co-authorship; the engine still owns schema/odds; the plan stays the player's, the GM advises.

## Guardrails held
Engine owns schema/odds/costs; GM advises, player decides. Part A is a one-function tune. Reused `parseGambitSteps` + `effectiveEnergyCost` + `gmAsk` — nothing reinvented. No mechanic change (surfacing heuristic + display + an advisory call). Additive (`gambitDraft.gmAdvice`, transient). Suites + parse_probe green. Never touched the ErikIAm pipeline.

## Verification
- `node tests/smoke.mjs` — 689 green (unchanged — the changed functions are app-level UI/heuristic); `node tests/parse_probe.mjs` — green (app module graph loads).
- **Boots clean on a fresh port** (v1.8.6, no console errors).

## Erik preview tests (Part B needs an API key for the GM advice)
1. "Play several scenes — the Plan hint now shows only when a real multi-step plan fits (a plan/scout/prepare-tagged or staged scene), not on every rich scene."
2. "Open the gambit builder, assess a plan — each step shows its energy cost and the total vs your energy (with a warning if over); tap **✦ GM, look at this** and the GM gives a concrete suggestion/warning on your draft before you commit."
