# Results — SNG-BATCH-8 (Fast high-impact)
Date: 2026-07-11 · Shipped v1.7.6 · Suite green (550 checks, +19 over BATCH-7) + parse_probe. Live-verified (fresh-boot clean, no console errors; shipped artifacts confirmed at HEAD via raw-source read). Status: **shipped, awaiting Erik preview-legs before complete_pending_review**. Only Aevi closes.

## Phase 1 — Gambit surfacing (SNG-031) + completion-XP wire (SNG-030 remainder): SHIPPED (v1.7.6)
- **SNG-030 fixed — the completion bonus was never paid.** `finishGambit` computes the outcome and then, on a **non-abandoned** completion, awards `rules.gambit.completionBonusXp` (10) to `character.xp`, runs `applyLevelUps` so a gambit can push a level *now* (not on the next rolled action), and surfaces `+N xp` in the gambit result label. An **abandoned** gambit still pays nothing. Strategist's extra adaptation point was already wired (`adaptationPointsFor`) and is now covered by a test so it can't silently regress.
- **SNG-031 — the fully-built gambit was hidden; now it surfaces when the scene warrants it.** `isGambitApt(turn)` marks a scene Plan-apt when it has **≥3 choices** AND any of: a `plan`/`scout`/`prepare`/`investigate`/`analyze` intent tag, **≥2** ability-relevant choices, or **≥4** non-trivial choices. Routine 1–2 choice scenes never trip it.
- **Surfacing (non-nagging):** a dismissible **gambit-hint chip** sits beside the freeform input on apt scenes; the ⚙ Plan button gets `.apt` emphasis only when apt. Dismissal (`gambitHintDismissed`) is respected until the next `startScene`, so it hints once per scene, not every turn.
- Live-verified: award + level-up + label semantics unit-tested exactly as `finishGambit` runs them; apt-detection boundaries unit-tested; chip + `.apt` render paths present in shipped `app.js`.

## Phase 2 — Narrative-driven time (SNG-032): SHIPPED (v1.7.6)
- **The GM now moves the world clock with the fiction.** GM contract gains `timeOps: {hoursPassed, why}`; rule text added: *"timeOps: TIME MOVES WITH THE FICTION — narration LEADS the world clock."* `timeOps` is registered in the `salvageOps` key list so a partially-malformed turn still recovers it.
- **`applyTurn` honors the declaration, clamped:** when `timeOps.hoursPassed` is finite it **REPLACES** the beat default (clamped **0.25–72h**); otherwise it falls back to the old `beat/sceneEnd + legacy timeAdvanceHours` behavior unchanged. Sleeping advances ~8h, a quick exchange ~0.5h, a multi-day trek clamps at 72h, a floor of 0.25h prevents zero-time turns. `advanceClock` already accepted fractional hours, so minutes-scale ticks work without engine change.
- **Backward-safe:** no `timeOps` = exactly the prior clock behavior; a declared value never *reduces* below 0.25h or exceeds 72h regardless of what the GM emits (engine owns the clamp — Design law 1).
- Live-verified: replace/fallback/clamp/floor all unit-tested against the mirrored `applyTurn` rule; contract + rule + salvage-key confirmed in shipped `gm.js`.

## Erik preview tests (per phase, live on v1.7.6)
1. **Gambit surfacing + reward:** "Walk into a scene with several planning-type options — a *Plan a gambit* hint shows up next to the text box; run a gambit to the end and you actually get the completion XP (it says +10) — while a plain one- or two-choice scene shows no hint."
2. **Narrative time:** "Sleep and the clock jumps about 8 hours; have a short conversation and only minutes-to-an-hour pass; the time-of-day the story describes matches the clock every turn — and travel/rest still advance time like before."

## Guardrails held
Design law 1 (engine owns the XP award, the level-up, and the time clamp; the GM only *narrates* and *declares* hours — the engine bounds them); additive schemas (`timeOps` is a new optional contract op; no stored-shape change — the clock already existed); reused `applyLevelUps`, `adaptationPointsFor`, `advanceClock`, `worldtime.ADVANCE` — nothing reinvented; no resolution-math change; this repo never touches the ErikIAm pipeline; suites + parse_probe green.

## spec_boundaries / flags
- **Ghost version-string in SYSTEM_SPEC.md line 8 — flagged, NOT silently edited.** The historical version chain records *"then v1.7.0: 4-tier stable-to-volatile GM prompt cache, 1h TTL"*. That 4-tier prompt-cache restructure was **specced but never built** — it was superseded by the `/model` switch + the SNG-BATCH-6 directive in the same session and no cache-tier code shipped (the caching work that *is* live is the v1.6.8 `cache_control`-on-content-blocks fix, not a 4-tier restructure). I did **not** rewrite the historical string because that ledger is Aevi-owned; surfacing it here for Aevi to correct or confirm. If the 4-tier restructure is still wanted, it needs re-pointing as its own batch — it is not in the codebase today.
- **Live chip render is heuristic-verified, not click-driven.** The gambit-hint chip only appears behind a real GM turn (needs an API key), so I could not headlessly click it into existence. Its gate (`isGambitApt`) and the completion-XP/timeOps math are unit-tested with the exact expressions `app.js` runs, and the render branches are confirmed present in shipped source — Erik's preview-leg 1 is the human confirmation that the chip *looks* right in place.

## Note on the dependency gate
Spec placed BATCH-8 to start "the moment BATCH-7 closes." BATCH-7 was shipped/green/live-verified and awaiting Erik's preview-legs (not yet Aevi-closed) when Erik directed "batch 8 is ready." BATCH-8 shares no code with BATCH-7's in-flight surface (gambit + time vs. identity/sync/resolver), so building now carries no dependency risk; both remain awaiting Erik legs in parallel.
