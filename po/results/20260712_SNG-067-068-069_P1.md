# Results — P1 the commit boundary (SNG-067/068/069, one defect)

Date: 2026-07-12 · v1.8.33 · npm test green · live-verified the full prologue on a fresh port. Status: **shipped, awaiting Erik leg.** Only Aevi closes.

## ROUND-2 substrate check first (as the ALERT asked)
The three tickets are one defect — *creation commits before the player confirms* — but **most of it was already fixed** by BATCH-10/SNG-063:
- **SNG-067 (redo can't repick the primary domain): already fixed.** `dom-reset` and `reveal-adjust` both clear all three domains → `renderDomainStep` is a genuinely free re-pick (verified working in the SNG-062/063 sessions). No change needed.
- **SNG-069 quick-start background: already a real pick** (the `c-bg` dropdown).

The **live gaps were all in the PROLOGUE path**, and this ships those:

## SNG-068A — the Silas bug (abilities from the guessed domains, none from the confirmed one)
`finishPrologue` set `abilities = state.prologue.granted` (what you USED) — never re-derived against the CONFIRMED domains, and never granted from the confirmed primary. A wright confirmed at the reveal ended up knowing nothing wright.
- New pure **`reconcileStartingAbilities(earned, domains, catalog, index)`** in `traditions.js`: **keeps every earned ability** (nothing stripped — "you did this, so you know this"), **flags any now-out-of-domain as `grandfathered`**, and **grants one lowest-tier ability from the confirmed primary** if the character has none there.
- The reconcile is surfaced as the **first-scene aside** (Law 9 — nothing silent): *"what you did in the fire stays with you, though it is not your people's craft: Disarming Strike."* / *"your people's craft is yours: you begin knowing …"*

## SNG-069A — the prologue auto-assigned a background
Now **CHOSEN**: a categorized background `<select>` (shared `backgroundOptionsHTML`, 6 categories — martial · practitioner · craft · learned · social · marginal) at the reveal, beside the domain confirm. The quick-start `c-bg` is upgraded to the same categorized picker. **Never gated by origin/domain** — a Cogitant duelist is legitimate.

## SNG-068B — the sidebar listed companions never met
The play sidebar rendered **every unmet companion as a "Travel with X" recruit button** (the full 9-roster as a menu). Removed — *a companion you have not met must not exist to you.* The roster appears only in the quick-start picker + the prologue's `companionBeat`. Empty state fixed to read from `character.companions` ("you travel alone — someone may yet fall in beside you").

## Verification
- `npm test` green — 4 new SNG-068A checks incl. the exact Silas scenario (earned the antipode's craft, confirmed a primary with none of it → the low-tier primary ability is granted; the antipode ability is kept + flagged grandfathered; a character who already knows their primary gets no extra grant).
- **Live-verified the full prologue** (fresh port, v1.8.33): played the Waystation opening → reveal offered the **6-category background pick** → chose **Duelist** (persisted as `duelist`, not the old auto `craftsman`) → confirm produced a character whose abilities reconciled with the grandfather note surfaced (*"…not your people's craft: Disarming Strike"*), **one** companion, and **no "Travel with" roster menu** in the sidebar. No console errors.

## spec_boundary
- **The prologue background pick pre-selects a neutral default the player confirms/changes** rather than deriving a suggestion from play — the prologue paths don't carry background hints, so a "you took things apart — Mechanist?" suggestion would be fabricated. The player clearly sees and chooses it (revealed-then-confirmed); if Aevi wants play-derived suggestions, the prologue paths need a `backgroundHint` field.

## Next
Per the current ALERT order: SNG-077 (the gambit hint is still constant) → SNG-066 (feedback) → SNG-073 (Skill Wheel) → SNG-058 (party leader); plus the new SNG-080 (the world must push).
