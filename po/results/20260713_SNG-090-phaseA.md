# Results — SNG-090 Phase A (the substrate foundation: pure engine + balance harness + load/CI)

Date: 2026-07-13 · v1.8.54 · npm test green (now incl. `balance_sim`) · live-verified. Status: **Phase A shipped, complete_pending_review.** No gameplay change yet — the resolve-chain wiring is Phase B.

Amendment 1 (the two-sided band, Erik's correction) is promoted (`the_substrate.json` at `1e3403e6`, SYSTEM_SPEC §9b at `34fccefe`). This builds the foundation the Round-2 blocked on: the pure factor, the balance harness that must exist before the curves are trusted, and the data load + CI.

## Shipped
**`engine/substrate.js` (pure)** — the two-sided BAND factor:
- Each tradition has a band `{center, width}`. **Inside** → full output. **Below** → steep **starvation**. **Above** → mild, floored **interference**. Carried charge adds to local density — it **rescues the starved and worsens the crowded**.
- `substrateVerdict({tradition, density, carried, data})` → `{factor, side, percent, chancePenalty, energyMult, off}`. `locationDensity(location, data)` → per-location override, else region density, else null.
- **Tunable constants** (`SUBSTRATE_TUNING`), validated by the harness — never eyeballed (the Round-2 blocker, honored).

**`tests/balance_sim.mjs`** — the balance harness owed since SNG-078, now required by SNG-090:
- Validates the substrate **design anchors as a real pass/fail gate** and prints the factor table.
- Reports the SNG-078 success-chance ceiling (attribute × difficulty) so Erik's balance call has data. Added to `npm test`.

**Load + CI** — `state.js` loads `the_substrate.json` → `CONTENT.substrateModel`; `content_ci` now requires **every location to resolve an effective density** (all 92 do).

## Verification (harness + live)
Harness anchors, all passing:
- **Seraph in the Quickwood: 13%** (starved, off) — the canonical near-powerless case.
- **Rootkin in the Gearlands: 69%** (crowded — impaired, never off).
- **Rootkin in the Quickwood: 100%** (tuned to thin ground — the band model corrects the old fuel-gauge "even Rootkin dimmed").
- **Seraph in the Lattice-Cities: 100%**, **Mason in the Given Land: 100%** (each at home).
- **Carried charge:** Seraph+Waystaff in Quickwood 13% → 100% (rescued); Rootkin+charge in Gearlands 69% → 67% (worsened — why the Rootkin find the trade ridiculous).

The factor table reads as a genuine two-sided map: the Continuous full in dense ground and starving toward thin; the Returned full at home/thin and crowded (but floored) in the deep density. Live: `substrateModel` loads (27 bands), `locationDensity(quickwood)=0.12`, `substrateVerdict(seraphic@quickwood)=13% starved/off`. No console errors; full suite green.

## Tuning (for the record, not eyeballed — set against the harness anchors)
`starveExp 1.15` (Seraph@Quickwood → 13%), `crowdSlope 0.75` + `crowdFloor 0.6` (Rootkin@Gearlands → 69%), `maxChancePenalty 65`, `energyK 0.6`, `gateBelow 0.18`. Re-run `npm run balance` after any change.

## Phase B (next — the gameplay wiring)
1. `substratePenalty` into `successChance` — **ability actions only** (weapon swings substrate-free, SNG-089); a hard **gate** at the extreme ("the lattice is too thin to work here"); optional **energy-cost multiplier**.
2. **SEPARATE from SNG-079's spectral-fit term** — both additive, both independently clamped, never folded (the accepted Round-2 finding).
3. **Receipts** ("The lattice is thin here — your craft runs at a fraction" / "The lattice crowds your signal") + GM context line + **map overlay** alongside `dangerLevel`.
4. **Carried-charge logistics** — the Waystaff as a spend/deplete/recharge nanite battery.
Phase B changes live resolution and needs careful in-play verification + a re-tune pass against fuller characters — hence split from this safe foundation.
