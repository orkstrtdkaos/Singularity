# Results — SNG-090 Phase B (substrate wired into live rolls)

Date: 2026-07-13 · v1.8.56 · commit `0c93ff6` · npm test green · live-verified fresh-port. Status: **shipped, complete_pending_review.**

## What Phase B does

Phase A (v1.8.54) built the pure engine + harness + load + CI with **no gameplay change**. Phase B makes the substrate **bite in live play** — a second, independent difficulty map alongside SNG-079 spectral fit.

The two-sided affinity band (design canon, Amendment 1): each tradition is tuned to a density BAND. Below it → steep starvation (a Continuous craft in thin ground is nearly off). Above it → mild, floored interference (a Returned craft in dense ground is impaired, never off). Carried charge adds to local density — it rescues the starved and worsens the crowded.

## Changes

- **`engine/resolve.js`** — `successChance({…, substratePenalty = 0})`. Subtracted as its own already-clamped additive term, positioned before the exhaustion penalty. **SEPARATE from the SNG-079 spectral term** (both additive, both independently clamped, never folded). A comment marks the boundary.
- **`app.js`**
  - `substrateForAction(choice, location)` — resolves the ability's tradition (`abilityTradition(ab) || ab.powerSystem`), the location's density (`locationDensity`), and carried charge (`carriedSubstrate`), returns the `substrateVerdict`. Returns `null` when there's no substrate model, no ability, no tradition, or no density (weapon/attribute actions are substrate-free).
  - `onChoice` — computes `substrate` for ability actions only. **Hard gate** when `substrate.off`: re-renders with an aside (*"The lattice is too thin here — X barely stirs (N% of its strength) and won't answer. Carry charge, or reach denser ground; steel and wit still work."*) and returns before rolling. Otherwise passes `substratePenalty: substrate.chancePenalty` to `resolveAction`.
  - After resolution, when the substrate bit (side starved/crowded): writes `resolution.substrate = {percent, side}` (the receipt) and sets `pendingSubstrateNote` (the GM note).
  - **Roll receipt** — a `.roll-affinity` line: *"the lattice is thin — your craft ran at N%"* / *"the lattice crowds your signal — your craft ran at N%"* + the spectral-fit ⓘ.
  - **Map overlay** — a density chip in the map-details head for visited places (`thin lattice` / `even lattice` / `dense lattice`, with the exact % + a one-line explanation in the title).
- **`engine/gm.js`** — `substrateDetail` added to the ctx; a **`## THE SUBSTRATE`** scene block instructs the GM to narrate the strain/interference and **never invent power the craft doesn't have this turn**.
- **`engine/substrate.js`** — `carriedSubstrate(character, itemCatalog, companions)` sums inventory `substrateCharge` + companion `substrateAura`, clamped [0,1]. Reads both inline item objects and id-referenced catalog items.

## Verification (live, fresh port 8351, real region keys)

Dynamic-imported `engine/substrate.js` + `the_substrate.json` in the booted page:

| Case | Result | Expected |
|---|---|---|
| Seraphic (Continuous) @ the_quickwood (d=0.12) | **13%**, `off: true`, penalty 57, starved | near-off, gated |
| Seraphic @ quickwood + carried 0.6 (Waystaff-like) | **100%**, `off: false`, full | battery rescues |
| Rootkin (Returned) @ the_gearlands (d=0.98) | **69%**, `off: false`, penalty 20, crowded | impaired, never off |

Anchors match the design canon exactly. Boot clean, no console errors. `npm test` green (Content CI + substrate anchor gate + SNG-078 ceiling report all pass).

## Boundaries / deferred (non-blocking)

- **`energyMult` computed but not applied.** `substrateVerdict.energyMult` is returned but not yet multiplied into `energyCost` — held for a balance call on whether thin/crowded ground should also drain energy faster. Flagged, not silently dropped.
- **Map overlay is a details-panel chip, not an SVG ring.** The chip satisfies the "overlay alongside dangerLevel" requirement; a full danger-ring-style substrate ring on the map SVG is a refinement.
- **Carried-charge needs content to bite.** The engine reads item `substrateCharge` and companion `substrateAura` today, but no content authors either field yet. The Waystaff (and charged reservoirs / living motes) want these before carried-charge does anything in real inventories — **Aevi content, flagged.**
- **Re-tune opportunity.** Phase B adds a fresh live term to `successChance`. SNG-078/079 balance is Erik's call; this is a natural moment to re-tune the whole chain together (harness now exists).

## Files touched
`engine/resolve.js`, `engine/substrate.js`, `engine/gm.js`, `app.js`, `index.html`, `po/ALERT.md`.
