# Findings — SNG-131 nanite-keepers innate precursor: ROUND-2 substrate verify → **BLOCKED on Aevi authoring + a ruling↔geometry contradiction**

Date: 2026-07-17 · HEAD `f91ca97` · CCode ROUND 2. **Status: NOT built — CCode-blocked. Needs Aevi authoring before any wiring.** Plus one inherited red fixed (below).

I did the ROUND-2 substrate verify the spec calls for before wiring. The verify found that **almost every CCODE open-question (OQ3) is either blocked on data Aevi hasn't authored, or is contradicted by the authored ring**. Building readers against imagined data would repeat the SNG-124 "named from memory, not from the data" failure — so I stopped at the boundary and am reporting precisely what's needed.

## What I VERIFIED at HEAD (file:line)
1. **`innatePrecursor` is a BOOLEAN on 2 ABILITIES, not an array on origins.** `content/packs/core/abilities/reach_demonic_angelic.json` — `the_kept_flame` (L1018) and `the_hollow_that_holds` (L1057) each carry `"innatePrecursor": true`. **No origin record carries `innatePrecursor` in any form** (checked all 27 in `origins.json`). The spec's own wiring table (SPEC L32/45) assumes the *origin*-level array shape `innatePrecursor: [abilityId…]` — that shape has no data.
2. **No engine reader exists for `innatePrecursor`** — grep of `engine/` + `app.js` is empty. Confirmed inert, as the ALERT honestly said.
3. **The 2 flagged capstones are `powerSystem: "reach_demonic_angelic"`, NOT `"precursor"`.** The precursor gate (`progression.js:253` `if (sys === "precursor") return precursorAccess.includes(ab.id) ? base : null`) and the `unlockPrecursor` writer (`app.js:2735`) both key on `powerSystem === "precursor"`. So these two abilities **do not route through the `precursorAccess` machinery at all** — they are already learnable at tier V by the ordinary domain/learn path (`gated:"learned"`, `levelReq:5`). Seeding them into `precursorAccess` would be a no-op. **The `innatePrecursor:true` flag currently drives nothing and, as targeted, would drive nothing.**
4. **`innateLivingCurrent` and `braidAffinity` are not authored anywhere** — spec/ALERT prose only; zero hits in content/, schemas/, engine/, app.js. There is no data to wire a reader against for either half (the nature-parallel and the center-braid).
5. **`precursorAccess`** = a plain array of ability-id strings on the character; seeded empty on load (`app.js:1103`, `reconcile.js:48`); the natural creation seam is `finish()` (`app.js:2020`, abilities seeded L2035, native grants applied L2069 via `applyNativeGrants`/`nativeGrantIdsFor` reading `rules.traditionNativeGrants`).
6. **Ruling 2's antipode claim is FALSE in the authored ring.** From `traditions.json` stations + `rootkin.distances`: `rootkin` = pos 17, its antipode is **`ashwarden`** (pos 5; the life↔death diameter). `seraphic` = pos 14, antipode `abyssal` = pos 2 (the angelic↔demonic diameter). **rootkin↔seraphic = 3 steps; rootkin↔abyssal = 9 steps.** So the living-substrate pole (rootkin) and the fabricated-substrate keepers (seraphic/abyssal) are on **different diameters — not opposite poles of one axis.** The spec's OQ (SPEC L26) anticipated this: "Confirm rootkin's ring position IS antipodal to the nanite cluster; **if not, note the geometry**." → It is not.

## What Aevi needs to author before CCode can wire (precise asks)
1. **The innate BASE abilities (ruling 1 — "innate access AS A BASE that grows").** For this to be mechanically real, each substrate-keeper origin needs one or two **`powerSystem: "precursor"` ability ids** designated as its innate base — the ones seeded into `precursorAccess` at creation, then deepened by the normal path. Author these as an **origin-level field** matching the spec's own wiring shape:
   - In `origins.json`, on `seraphic` and `abyssal` (and any other keeper you designate): `"innatePrecursor": ["<a real precursor-powerSystem ability id>", …]` — **precursor abilities, not the reach_* capstones.** 1–2 each (the "a FEW, not the tier" guard).
   - The two existing tier-V capstones (`the_kept_flame`/`the_hollow_that_holds`) are the **path-earned variant** and already work as-is (learnable at tier V); their `innatePrecursor:true` boolean can stay as documentary, but it is not the creation-seed vector. If you want them to be the *only* mechanism (no creation base), say so and SNG-131's "innate base at creation" (ruling 1) becomes a no-op — the capstones already deliver the path-earned half.
2. **`innateLivingCurrent`** (ruling 2 nature-parallel) — if wanted, author the parallel: the living-substrate ability set + an origin-level list on rootkin/nature-keeper origins. Nothing exists yet.
3. **`braidAffinity`** (ruling 3 center-braid) — if wanted, author the field on folk/Valley origins + define the concrete bonus (which braid/diameter abilities get cheaper/earlier, or the unique "hold both" move). Nothing exists yet.
4. **Reconcile ruling 2 with the ring.** rootkin is NOT antipodal to seraphic/abyssal in the authored geometry. Options: (a) **recommended — adjust the framing**: treat the two as *distinct* deep-power families (fabricated vs living substrate) without claiming they are antipodal poles of one axis; (b) re-position the ring so rootkin sits opposite the nanite cluster — **CCode advises against (b)**: ring positions feed all `ringDistance`/`antipodeOf`/domain-access math and every existing character's kin gates (SNG-125); moving them is high-blast-radius. If (b) is truly wanted, it's its own ticket with a migration + balance pass.

**Once #1 lands (origin-level precursor-powerSystem arrays), the CCode wiring is a clean, verifiable ~10-line change:** seed `character.precursorAccess` from `originRecord(origin).innatePrecursor` in `finish()` (validating each id is actually `powerSystem:"precursor"` so a mis-authored id can't create a false "granted" state), plus a version-gated retro for existing keeper characters (mirroring `retroNativeGrants`). I did not build it ahead of the data — no origin carries the field, so it could not be verified end-to-end, and building against the wrong (reach_*) ids would silently grant nothing.

## Inherited red fixed this pass (SNG-130 content) — committed `f91ca97`
Aevi's SNG-130 tier IV–V capstone authoring used **descriptive `harmRung` words outside the canonical SNG-089 ladder** (`none < incapacitating < damaging < lethal`), failing the content_ci harmRung-validity check on **13 abilities** across 6 `reach_*` files and **red-gating the whole suite** (Aevi's "all parse-verified" pass didn't run content_ci). Mapped each to the nearest canonical rung by the ability's own fiction — `binding → incapacitating` (4), `moderate → damaging` (2), `severe → damaging` (7). Enum correction only, no prose touched; conservative (never auto-escalated to `lethal` — none of the 13 describe killing a person; several explicitly cannot touch the living). **Flag for Aevi:** review the 13, especially `the_last_light` (the one arguably-lethal Blazeborn capstone). Suite green after the fix.

## Recommendation
- **SNG-131:** author asks #1–4 above (the precursor-powerSystem base ability ids per keeper origin are the load-bearing one; the living-current/braid halves and the ruling-2 reconciliation can follow). Then CCode wires + verifies against real data.
- **Meanwhile, SNG-138** (Brayden's ronin recurrence wiring) is **fully ready** — arc + 6-challenger pool authored, manifest-registered, and its pool now has a clean `content.challengerPools` home (SNG-137). That is the buildable ticket in the queue right now.

## Files touched
Content fix only: `content/packs/core/abilities/reach_concrete_abstract.json`, `reach_dark_light.json`, `reach_demonic_angelic.json`, `reach_destruction_creation.json`, `reach_emotional_logical.json`, `reach_falsehood_truth.json` (harmRung enum corrections). **No SNG-131 wiring written** (blocked).
