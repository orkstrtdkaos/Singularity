# BATCH-11 — BUILD SHIPPED (the buildable lanes, in full)

| | |
|---|---|
| **Task** | BATCH-11 build per PM authorization ("run the ready spec and updates in full") |
| **Author** | CCode · 2026-07-18 |
| **Status** | `complete_pending_review` — Aevi closes, on the reproduced symptom |
| **Versions** | v1.8.105 → v1.8.108 (six ships, each committed + pushed + suite-green) |
| **ROUND 2 basis** | `po/results/20260718_BATCH-11_round2_substrate.md` (same session, answered first) |

---

## What shipped, in build order

### 1 · SNG-146a — the Law 7 lost-update, CLOSED at the transport (v1.8.105, `540d0de`)

Built first, against the PO's proposed sequence — an active data-loss defect outranks architecture. `pushSceneWithMerge` now runs its mutate INSIDE `pushMergedFile`'s callback, so **the sha and the content come from the same read**; a concurrent write raises a real 409 and the retry re-merges onto the winner.

**Proven live, not code-read:** `scripts/verify_scene_merge.mjs` (persistent helper) seeds a scratch scene against the real repo, fires two concurrent `pushSceneWithMerge` calls, asserts BOTH beats survive, and cleans up. Run GREEN this session — the spec's exact acceptance criterion. Rerun it after any change to the scene write path: `SINGULARITY_PAT=<pat> node scripts/verify_scene_merge.mjs`.

**Honest residual (now in §18):** a `GH_TIMEOUT` after a server-side apply retries safely for beats (idempotent by `(by,at)`), but `setEncounterState`/`removeMember` remain last-writer-wins on their fields.

### 2 · SNG-146b/c — scene lifecycle + open-scene index (v1.8.105, `7945c30`)

`closedAt` on scenes; the last member leaving closes (pure, in `removeMember`); idle-past-72h expires **lazily** — no write needed to retire an abandoned scene. `listScenesAt` reads `world/scenes/_open_index.json` (maintained fire-and-forget at the single write choke point, self-pruning via the same TTL) — one small fetch, cost independent of scenes-ever-created, and **the bound applies AFTER the open-filter** (the old `slice(-5)`-before-filter could report "nothing joinable" while live scenes existed). Scene file stays the truth; index is a hint. Live-verified: index entry appears on push, clears on close.

### 3 · SNG-146f — personalArc is startable (v1.8.105, `dc27bff`)

The ROUND 2 finding, fixed: the quest-log listing spliced `character.personalArc` into its array but the start handler looked the def up in `CONTENT.quests` only → `def === undefined` → throw. Start lookup now searches the same spliced array; `startStructuredQuest` refuses cleanly on a missing def. **§146d upgrades from WARN/unverified to FIXED** — the arc reaches the play-loop GM via `structuredQuestsForGM` once taken.

### 4 · §23 — the GM Context Registry + wiring audit (v1.8.105, `d1e7aaf`)

**Law 16 ratified by PM this session** (the "run in full" authorization) and mechanical:

- `engine/gm_registry.js` — ONE declared table, 44 rows: `{key, builder, carries, reachedBy, spec, views, build(env)}`. app.js assembles the GM context by **iterating it at all four call sites** (turn/ask/quest/gambit — ROUND 2 found 4, not the spec's 3) via `gmEnv()` + `assembleGMContext(view, env)`. Prompt ordering stays owned by `gm.js tierParts` — the registry is a bag of keys, which is exactly what makes one table serve four sites. Site divergences ride env overrides; runGM's one-shot ephemera are pre-consumed and carried in `env.ephemera` so no row can double-fire.
- `tests/wiring_audit.mjs` **joins `npm test`** and FAILS on: registry ↔ tierParts parity drift (both directions), a hand-listed ctx literal at any gm call site, SYSTEM_SPEC header count drift, and skill-integrity ratchet regression. Advisory orphan-export sweep with `// registry:internal` opt-out (the LEGEND_BEATS false-positive class, per ROUND 2).
- **The gate works on its own author:** it red-gated my SNG-145 ship for an undercounted module header (50 vs 51) — the exact drift class it was built to catch. Fixed forward same session (`SYSTEM_SPEC` note carries the story).

### 5 · SNG-145 — intent confirmation for costly acts (v1.8.106, `728030a`)

Law 9 extended into the play loop, in the shape ROUND 2 recommended — **escrow-as-engine-state, never a held turn**:

- **harm** — a *declared* lethal-rung cast gates **pre-dice in `onChoice`** (before energy, before the roll, before the GM). Deduped per encounter/scene. Resume = the escrowed CHOICE itself (plain JSON), annotated (`intentRung` + `intentNote`) and re-entered through `onChoice` — the normal path runs exactly once. The confirmed intent binds narration via `resolution.intentNote` ("the target SURVIVES this beat" / "the blow may kill").
- **departure** — a cross-REGION travel intent gates before the GM is called; **the moveTo always-emit contract is untouched, verbatim** (smoke-tested against the contract string). Intra-region never gates; null regions fail OPEN; minted places inherit the origin's region so they can never spuriously gate. Declining just… stays — nothing was committed to undo.
- **irreversible** — `impactsLocal: true` ledger events HOLD in escrow; narration stands, shared-world propagation waits for the player's confirm. Unanswered never propagates — nothing is imposed on another player's world by inaction.
- **`offerIntent` op** — for the fiction-recognized cases the engine can't see (a freetext strangling): contracted in gm.js with the **gambit discipline** (emitting it forbids emitting the act's effects that turn; "hold the scene at the brink"), RARE by contract (never re-ask a stated intent), salvage-whitelisted, sanitized repair-not-wish (bad shape → no gate, never a guessed gate).
- **Lenient BY CONSTRUCTION**, not by timer: nothing commits until answered, and nothing IS the gentle branch. Escrow persists on `character` — a closed tab re-renders the card. `onFreeform` routes through `onChoice`, so freetext inherits both engine gates.

### 6 · SNG-148 — waygates (v1.8.107, `dbe6e2f`)

PM's BOTH ruling, composed: *knowledge* (destination in `knownPlaces`) + *skill* (`wayfaringTier` = wits/2 + traveled-breadth bonus, floor 1). Both → **named** gate; either alone → **the Crossing hub**; not-at-a-gate / non-gate target → null = **standard travel, a routing outcome never a failure**. Full §23 chain: engine `waygate.js` · consumer map **◈ control** (bypasses `connections[]` — that's the point; the click is the confirmed intent; transit = real hours through `travelTo`) + gm.js WAYGATE block · registered `waygateDetail` (44th row, parity green) · reachable map + GM offer (offer-lightly discipline in the block text) · contracted §9. Verified on served bytes: Axis Gate → hub routing live in browser.

**Content seed (minimal, Law 2):** `the_crossing` = hub (tier 1), `the_axis_gate` = gate (tier 2); fields documented in `location.schema.json`. **→ PO content lane: per-region gates.** The network is live but small.

### 7 · SNG-147a/d — skill integrity (v1.8.105 + `ccb6486`)

- **`challengeProfile` RETIRED** — 280 records cleaned. ROUND 2 measured zero runtime consumers, zero CI validation, and an abandoned `cognitive/physical/social` triad on ~89 records. A stay-retired gate in the audit fails the build if it reappears.
- **`challengeTypes` HELD, not retired** — `content_ci.mjs:97`'s FIGHT/notFor lint is the only automated guard for the 147c failure class until the PO's grants-rewrite ships. Retire it after 147c, and re-home the lint.
- **147d standing ratchet** (in `npm test` via the audit): `tests/wiring_baseline.json` pins missing-`harmRung` 140 / non-canon challenge types 89 / combat-claimed-not-taught 105 — **counts may only DECREASE**; invalid `harmRung` enums are always zero (the PO's 13-invented-values incident is machine-checked from here). Deliberate improvement re-baselines with `UPDATE_WIRING_BASELINE=1`; hand-editing upward is forbidden by the file's own note.

### 8 · SYSTEM_SPEC (v1.8.108)

Law 16 in §2 (ratified, with the learned-failure note) · new §23 (chain, registry, gate, authoring rule) · §9 waygates · §11 `offerIntent` + ledger-escrow rows · §18 same-read law + lifecycle/index · §22 stateOps claim corrected **with provenance** (superseded same-day by SNG-070; six days stale; propagated into a spec — the documentation-layer version of the Law-16 failure) + BATCH-11 resolved line · header counts machine-gated (52 modules / 285 abilities at ship).

---

## Verification (Law 12 — runtime first)

- **`npm test` green at every ship** — smoke (incl. ~40 new BATCH-11 tests) + parse + content_ci + balance + skill-battle + **wiring audit** (now sixth in the chain).
- **Live two-client acceptance** for 146a/b/c against the real repo (both beats survive; index lifecycle) — the spec's own criterion, not a code read.
- **Browser-verified on served bytes, port 8105:** boot-clean each ship; registry live (44 rows, view splits 44/17/11/11); intent gates probed live; waygate routing (axis→hub) + GM block live. (One stale-fetch false alarm — the served bytes were fresh, the browser cache wasn't; re-probed with cache-busters. The stale-preview lesson generalizes to `fetch`.)

## Not built, and why (spec's own gating)

- **146e** (joint participation) — design-only this batch, per spec.
- **SNG-149 Coliseum** — depends on 146e. **ROUND 2 stands: `arcSeed` cannot supply grid axes** (free-text prose); `FUNCTION_FAMILIES` (8 closed values = two 4-slot axes) + tradition-ring geometry is the evidence-backed source when 149 is specced.
- **SNG-150 Council** — depends on 149's world-standing. ROUND 2 stands: reuse the `legends.json` roster (genuine Epic-Hero data layer, already NPC-hydrated), never `legendSurfacing` (a cameo dice-roller); substantial authoring lane regardless.
- **147b harmRung backfill (140) + 147c grants rewrites (71)** — PO/content lane per spec §4. The ratchet will show each backfill batch as a decreasing count; make `harmRung` required-and-enum-gated once 147b completes (flip the ratchet entry to a hard zero).

## Spec boundaries

- **Content edits made in the content lane, minimally and flagged:** waygate flags on 2 locations (the system needs one seed to be REACHABLE — link 4 — and the Axis Gate is literally named for it) + the mechanical `challengeProfile` deletion (147a's ruled retirement, not authoring). Both are Aevi's to re-shape.
- **`.claude/launch.json` (ErikIAm repo)** — Singularity preview port moved 8321 → 8105 for the fresh-port discipline.
- **The legends detector gap** (ROUND 2: `passing_advice`/`villain_escalation` undeployable — two authored anchors can never fire) — NOT built this batch; it's a design-adjacent call on when villains escalate. Flagged as a small standalone ticket.

*— CCode. Six ships, each pushed green. Only Aevi closes, on the reproduced symptom.*
