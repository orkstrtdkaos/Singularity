# SNG-215 — the play-screen UX pass (ALL FOUR surfaces shipped, live-verified)

**CCode · 2026-07-23 · v1.8.232→235 · suite + wiring-audit green · §A/§B/§C all confirmed live on a fresh port.**
`status: complete_pending_review`

Four surfaces, all built and **live-verified**. §C's authored `trait_readouts.json` content is the one thing
left for Aevi — the derived fallback carries it until then.

---

## §A — the ability sidebar (shipped, live)

**§A2 group by FUNCTION FAMILY:** the sidebar now groups owned crafts by their primary function family
(HARM/RESTORE/PROTECT/KNOW/SHAPE/INFLUENCE/MOVE/SUSTAIN via `familiesOfAbility` + the `FN_INDEX` already built
at boot), each header carrying the family glyph + the world's function colour, instead of the flat tradition
scroll. Grouping is a VIEW — every row still shows rank, cost, practice; braids keep their group.
*Live confirm: the sidebar rendered `◯ Know (1)`, `⬢ Influence (1)`.*
- **ROUND 2 Q3 answered:** function-family is NOT a bare field on the ability record — it's the
  `familiesOfAbility(ab, FN_INDEX)` join, and `FN_INDEX` was already built at boot, so §A2 is pure render, no
  new data.

**§A1 player-set BOOST (a nudge, never an override):** a ✦ toggle per craft row writes
`character.boostedCrafts`; a delegated handler persists it with a targeted DOM update (no full re-render). The
boost feeds BOTH suggestion surfaces — the level-up reasoned pick (the caller now *passes* `boosted`;
`suggestNextCrafts` already declared+rendered it but was never fed) and the SNG-214 choice pre-fill (the rule
now FAVOURS a boosted craft when it fits; the TOOLKIT block surfaces boosted crafts so the choice author reads
them). Guard everywhere: a thumb on the scale when a craft FITS — never a force, never a roll change.
*Live confirm: 2 ✦ toggles rendered.* Depends on SNG-214 (shipped).

## §B — inventory is a BAG (shipped, live)

The inventory screen is now a GRID of kind-icon tiles (⚔ weapon · ⚒ tool · 🧪 consumable · 📜 quest · ◈ relic),
grouped by kind, pinned items marked + floated first, responsive columns. Tapping a tile opens a DETAIL POPUP
(modal) that reuses `itemCard(open)`: the larger generated item image, full description, bonus/effect
mechanics, a grown story-item's stage, and Use/Name/Drop + pin — closes on ✕ or backdrop. Reuses the ONE
shared item binding; no new action code.
*Live confirm: `⚔ Belt Knife 📌`, `⚒ Traveler's Pack`, `🧪 Waterskin` tiles grouped by kind; opening Belt
Knife showed its generated image + "A plain working blade…" + helps-with tags + the three actions.*
- **ROUND 2 Q1 answered by Erik:** icons in the grid (fast, no quota), a generated image on the detail popup
  only (generate-on-open via the existing `itemImage`). Best of both.

## §C — merge Chronicle + Character sheet (shipped, live)

Erik's calls: **tabs within one screen**, and **start the shell now** (derived fallback until Aevi's content).

**§C-1 — traits read in two registers (the heart):** `traitReadout(kind, id)` — Aevi's authored
`CONTENT.trait_readouts` wins; else a DERIVED fallback from data that already exists: background → description
+ affinity + grantsAptitudes; origin → whyYouAreHere + native craft/pole; tradition → the FUNCTION FAMILIES it
grants (derived by unioning its abilities' families); form → "cosmetic, leads the portrait, no mechanical
effect"; aspiration → progress/ripe + free-learn + it weights GM suggestions. Origin, background, form, each
domain/tradition, and each aspiration are TAPPABLE (dotted underline) → the readout on the shared popover.
*Live confirm: tapping "craftsman" showed "Making, mending, material sense…" + "MECHANICS: Helps with EXPLORE,
INVESTIGATE challenges (a soft edge, never a gate). Grants…".*
- **ROUND 2 Q3 answered:** function-family is the `familiesOfAbility` join, no bare field. Q1 (§B) answered by
  Erik (icons + detail-image). Q2 (§C layout) answered by Erik (tabs). Q4 (trait set): background, origin,
  tradition, form, aspirations — the set the derived fallback covers; Aevi authors readouts for these.

**§C-2 — the merge:** a `characterTabBar` makes the character surface ONE screen with **Traits | Chronicle**
tabs (switching between `renderCharacterScreen` and `renderChronicle`). Dedup: the standalone "The Chronicle"
button is gone (the tab replaces it), and the Traits "Story" block no longer duplicates the chronicle's lived
paragraph. *Live confirm: [Traits* | 📜 Chronicle]; clicking Chronicle switches under the same name-header to
the chronicle sections, Traits tab to return.*

**The content shell is READY** (`trait_readouts.json` wired into `loadContent` — optional load, empty
fallback). Aevi drops the file + a manifest `provides.rules` entry, and authored readouts override the derived
text with no code change.

## Aevi's remaining piece
Author `content/packs/core/rules/trait_readouts.json` (shape `{ readouts: { background:{id:{lore,mechanics}},
origin:{…}, tradition:{…}, form:{…}, aspiration:{…} } }`) + register it in the core manifest's `provides.rules`.
The staged `po/staged_content/tradition_motivations.json` (keyed by traditionId, currently unloaded) is a
ready seed for the tradition readouts. Until then the derived fallback carries every trait.

## Honest bounds
All four surfaces are **Tier-1 + a live browser confirm** (Node suite + fresh-port renders of the grouped
sidebar, the boost toggles, the bag grid + detail popup with a real generated image, the trait readouts, and
the tab switch). The boost's *effect on live suggestions* and the GM's *use of authored readouts* are Tier-3
(need an API turn / Aevi's content).

*— CCode. All four of Erik's surfaces landed and I watched each work: the kit reads by function with a star to
lean into one, the inventory is a bag of objects each opening to its own image and mechanics, every trait taps
through to what it MEANS and what it DOES, and the sheet and chronicle are one surface with two tabs. The last
thread is Aevi's words for the trait readouts — the ground they stand on is in, and the derived fallback holds
until they arrive.*
